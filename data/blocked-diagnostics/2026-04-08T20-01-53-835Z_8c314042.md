# Workflow Blocked Diagnostic

## Summary
- **Title:** Sentry fix [investor-pipeline]: Frontend closePanel null reference
- **ID:** 8c314042-73c0-4112-b0eb-87e41beee502
- **Blocked at:** 2026-04-08T20:01:53.864Z
- **Reason:** Phase 'implement' job 3d7520ee failed (timeout)
- **Phase:** implement
- **Cycle:** 1/5
- **Milestones:** 0/3
- **Implementer model:** claude-opus-4-6
- **Reviewer model:** codex
- **Worktree:** /Users/barneyhussey-yeo/GitHub/personal/.orchestrator-worktrees/investor-pipeline/wf-8c314042 (branch: workflow/sentry-fix-investor-pipeline-frontend-cl-8c314042)

## Job History (last 10)
| ID | Phase | Status | Model | Title |
|----|-------|--------|-------|-------|
| 860e32b5 | assess | done | claude-opus-4-6 | [Workflow C0] Assess |
| 9459ff07 | review | done | codex | [Workflow C1] Review |
| 3d7520ee | implement | failed | claude-opus-4-6 | [Workflow C1] Implement |

## Failed Jobs (last 5 with details)
### [Workflow C1] Implement
- **Job ID:** 3d7520ee | **Agent ID:** 36694f7b
- **Phase:** implement | **Model:** claude-opus-4-6
- **Exit code:** n/a | **Turns used:** n/a | **Cost:** $n/a
- **DB Error:**
```
Agent session ended without a final result event or new commits.
```
- **Agent output (last lines):**
```
Now let me find where `showDetail` actually opens the panel (shows it):
Four locations total — lines 662-663 (opening) and 850-851 (closing). Let me see the context around lines 662-663:
Now I have the full picture. There are 4 unguarded `.classList` accesses on potentially null elements:
- Line 662: `document.getElementById('detail-panel').classList.remove(...)` — opening
- Line 663: `document.getElementById('panel-backdrop').classList.remove(...)` — opening
- Line 850: `document.getElementById('detail-panel').classList.add(...)` — closing
- Line 851: `document.getElementById('panel-backdrop').classList.add(...)` — closing

Now let me lock the file and apply the fix.
Let me verify the edits look correct:
Both edits look correct. Now let me run the tests to verify nothing is broken.
1190 passed, 2 failed. The 2 failures are pre-existing — they're in `test_anthropic_client.py` and are due to a real API key leaking through the environment rather than the mock taking effect. These are unrelated to my change (they exist on the branch already).

Let me verify these failures exist before my change:
Confirmed — the 2 failures are pre-existing and unrelated to my change. My change introduces no new failures.

Now let me commit the fix.
```


## Total Job Stats
- Total: 3
- Done: 2
- Failed: 1
- Cancelled: 0
- Success rate: 67%

## Git State
### Working tree status
```
M static/shared.js
```

### Last 3 commits
```
0a4b747 fix: add deploy lock, stop-before-mutate, and compile gate to watch-deploy
e5f6927 fix: stop dashboard before git mutations in poll-deploy.sh
d052993 fix: skip startup when deploy lock is held
```

## Latest Worklog Entry
```
(no worklog found)
```

## Plan (truncated)
```
# Plan

## Goal
Fix the null reference error in `closePanel()` (`static/shared.js:849-851`) that crashes on pages which load `shared.js` but do not render the shared detail-panel DOM (for example `/firms`, `/firm/<name>`, and `/calendar`).

## Assessment
- `closePanel()` at `static/shared.js:849-851` unconditionally calls `.classList` on `document.getElementById('detail-panel')` and `document.getElementById('panel-backdrop')`.
- A global `keydown` listener at line 855 calls `closePanel()` on every Escape press, on every page that includes `shared.js`.
- `showDetail()` at `static/shared.js:343` also unconditionally accesses the same two elements when opening the shared panel; the current plan incorrectly refers to a nonexistent `openFirmPanel()` function.
- Templates that render the shared panel elements: `dashboard.html`, `insights.html`, `briefs.html`, `diligence.html`, `rejections.html`, `process.html`.
- Templates that load `shared.js` without rendering the full shared panel: `firms.html`, `firm.html`, and `calendar.html` (`calendar.html` has `#panel-backdrop` for its own panel flow, but no shared `#detail-panel`).
- Existing tests only verify that `shared.js` is served or that an Escape handler string is present; they do not currently cover the risky combination of pages loading `shared.js` while omitting the shared panel DOM.
- There is no JS runtime/browser test harness in this repo, so the regression test needs to be a targeted pytest assertion over rendered HTML and/or the guarded JS source rather than a new end-to-end browser test.

## Milestones
- [ ] **M1: Guard shared panel DOM access in the real code paths** [S] — Update `static/shared.js` so both `closePanel()` and the panel-opening path inside `showDetail()` safely handle missing `#detail-panel` and `#panel-backdrop` elements. Prefer caching the queried elements and either early-returning or conditionally mutating `classList` only when the elements exist. Acceptance: `shared.js` no longer dereferences null for either element in the open or close flow, and pages that do render the shared panel keep the same visible behavior.
- [ ] **M2: Add regression coverage for pages that load shared.js without the shared panel** [S] — Add focused pytest coverage that proves the affected page class exists and the guard is intentional. At minimum, cover `/firms`, `/firm/TestFirm`, and `/calendar` as pages that include `shared.js` but do not render the shared `#detail-panel`, and add a narrow assertion on the served `shared.js` source that the shared panel access is guarded rather than unconditional. Acceptance: the new/updated tests fail against the current unguarded implementation and pass once M1 is applied.
- [ ] **M3: Verify and ship the scoped fix** [S] — Run the relevant pytest subset covering the touched frontend asset/routes, then commit, push, and open a PR from `workflow/sentry-fix-investor-pipeline-frontend-cl-8c314042` with only the intended `investor-pipeline` changes. Acceptance: the
```
