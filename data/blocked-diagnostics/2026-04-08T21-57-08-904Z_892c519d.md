# Workflow Blocked Diagnostic

## Summary
- **Title:** Sentry fix [hurlicane]: WorkflowBlocked: Workflow blocked: Autonomous Agent Run: ## Error Boundaries & R
- **ID:** 892c519d-9590-4e50-8536-34aa5e53982a
- **Blocked at:** 2026-04-08T21:57:08.947Z
- **Reason:** Workflow stuck after failed implement job bf961dd5
- **Phase:** implement
- **Cycle:** 1/5
- **Milestones:** 0/4
- **Implementer model:** claude-opus-4-6
- **Reviewer model:** codex
- **Worktree:** /Users/barneyhussey-yeo/GitHub/personal/.orchestrator-worktrees/hurlicane/wf-892c519d (branch: workflow/sentry-fix-hurlicane-workflowblocked-wor-892c519d)

## Job History (last 10)
| ID | Phase | Status | Model | Title |
|----|-------|--------|-------|-------|
| 2f114f3e | assess | done | claude-opus-4-6 | [Workflow C0] Assess |
| 56e20d34 | review | done | codex | [Workflow C1] Review |
| 894cfe99 | implement | failed | codex | [Workflow C1] Implement |
| bf961dd5 | implement | failed | claude-opus-4-6 | [Workflow C1] Implement (fallback) |

## Failed Jobs (last 5 with details)
### [Workflow C1] Implement
- **Job ID:** 894cfe99 | **Agent ID:** 7009713c
- **Phase:** implement | **Model:** codex
- **Exit code:** -1 | **Turns used:** n/a | **Cost:** $n/a
- **DB Error:**
```
Reading prompt from stdin...
```
- **Agent output (last lines):**
```
(no log output available)
```

### [Workflow C1] Implement (fallback)
- **Job ID:** bf961dd5 | **Agent ID:** 6f8777c2
- **Phase:** implement | **Model:** claude-opus-4-6
- **Exit code:** n/a | **Turns used:** n/a | **Cost:** $n/a
- **DB Error:**
```
API Error: 429 {"type":"error","error":{"type":"rate_limit_error","message":"This request would exceed your organization's rate limit of 30,000 input tokens per minute (org: 62dc307d-268c-4e85-b5a6-86a2613aab61, model: claude-opus-4-6). For details, refer to: https://docs.claude.com/en/api/rate-limits. You can see the response headers for current usage. Please reduce the prompt length or the maximum tokens requested, or try again later. You may also contact sales at https://claude.com/contact-sales to discuss your options for a rate limit increase."},"request_id":"req_011CZs3udYrvKkeCX7XWyzBK"}
```
- **Agent output (last lines):**
```
[ERROR] rate_limit
[ERROR] unknown
[RESULT] "API Error: 429 {\"type\":\"error\",\"error\":{\"type\":\"rate_limit_error\",\"message\":\"This request would exceed your organization's rate limit of 30,000 input tokens per minute (org: 62dc307d-268c-4e85-b5a6-86a2613aab61, model: claude-opus-4-6). For details, refer to: https://docs.claude.com/en
```


## Total Job Stats
- Total: 4
- Done: 2
- Failed: 2
- Cancelled: 0
- Success rate: 50%

## Git State
### Working tree status
```
(clean)
```

### Last 3 commits
```
023a923 fix: fail fast when job work_dir does not exist (#40)
65e1aea test: cover PTY spawning-state cleanup
7f24e00 fix: clear standalone spawning state after monitor handoff
```

## Latest Worklog Entry
```
(no worklog found)
```

## Plan (truncated)
```
# Plan

## Goal
Fix the `spawnSync /bin/sh ENOENT` error that blocks workflows when a worktree directory has been deleted before phase spawning.

## Assessment
The Sentry error is triggered because `spawnPhaseJob` still performs a direct `ensureWorktreeBranch(worktree_path, worktree_branch)` check before spawning the next phase. That bypasses the broader `verifyWorktreeHealth(...)` path already used by `reconcileRunningWorkflows` and `resumeWorkflow`, so a deleted worktree never gets the existing recreate-or-repair logic before the workflow is marked blocked.

Review improvements applied to the plan:
1. Prioritize the real failing path first: switch `spawnPhaseJob` to `verifyWorktreeHealth(...)` before touching the helper. That addresses the exact Sentry regression and reuses existing self-healing behavior.
2. Tighten the test milestone to the correct suites and assertions. `ensureWorktreeBranch` tests already live in `src/test/workflow-dedup.test.ts`, while `src/test/worktree-health.test.ts` covers `verifyWorktreeHealth`. The plan should extend those targeted suites instead of a vague `or similar` location.
3. Sharpen acceptance for the helper fix so it proves the missing-directory case never shells out. A clear error string alone is not enough if `execSync` still runs and can surface `ENOENT`.

A secondary follow-on path remains in `pushAndCreatePr`, which still calls `ensureWorktreeBranch` directly. Hardening the helper keeps that path from surfacing the same misleading missing-worktree error text even though it is not the workflow-blocking path from this Sentry event.

## Milestones
- [ ] **M1: Route pre-spawn verification through `verifyWorktreeHealth`** [S] — In `src/server/orchestrator/WorkflowManager.ts`, replace the direct `ensureWorktreeBranch(...)` call inside `spawnPhaseJob` with `verifyWorktreeHealth(workflow.worktree_path, workflow.worktree_branch, workflow.work_dir)`. Preserve the existing blocked workflow behavior for unrecoverable failures, but let deleted or damaged worktrees use the existing recreate/repair logic before the workflow is blocked. Acceptance: a missing worktree can be recreated before a review/implement job spawn, and unrecoverable failures still set a clear `blocked_reason` from the health check.
- [ ] **M2: Short-circuit `ensureWorktreeBranch` when the worktree path is missing** [S] — Guard `ensureWorktreeBranch` with an `existsSync(worktreePath)` check before any `execSync` call. Return `{ ok: false, error: "Worktree directory does not exist: <path>" }` when the directory is absent. Acceptance: the helper never invokes git for a missing path, and remaining callers receive deterministic non-`ENOENT` text.
- [ ] **M3: Add focused regressions in the existing workflow/worktree test suites** [M] — Extend `src/test/workflow-dedup.test.ts` to prove phase handoff now uses `verifyWorktreeHealth`, including a deleted-worktree recovery path and an unrecoverable failure path that still blocks with the health-check error. E
```
