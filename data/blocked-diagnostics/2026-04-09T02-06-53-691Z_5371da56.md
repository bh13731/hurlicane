# Workflow Blocked Diagnostic

## Summary
- **Title:** Sentry fix [hurlicane]: WorkflowBlocked: Workflow blocked: Sentry fix [hurlicane]: WorkflowBlocked: Work
- **ID:** 5371da56-0cb0-4152-b9fb-51a753c2a940
- **Blocked at:** 2026-04-09T02:06:53.716Z
- **Reason:** PR creation failed — worktree preserved for retry at /Users/barneyhussey-yeo/GitHub/personal/.orchestrator-worktrees/hurlicane/wf-5371da56
- **Phase:** idle
- **Cycle:** 3/5
- **Milestones:** 3/3
- **Implementer model:** claude-opus-4-6
- **Reviewer model:** codex
- **Worktree:** /Users/barneyhussey-yeo/GitHub/personal/.orchestrator-worktrees/hurlicane/wf-5371da56 (branch: workflow/sentry-fix-hurlicane-workflowblocked-wor-5371da56)

## Job History (last 10)
| ID | Phase | Status | Model | Title |
|----|-------|--------|-------|-------|
| 7f0b19e6 | assess | done | claude-opus-4-6 | [Workflow C0] Assess |
| 8bad1917 | review | done | codex | [Workflow C1] Review |
| 24c7f385 | implement | done | claude-opus-4-6 | [Workflow C1] Implement |
| 7d8e665a | review | done | codex | [Workflow C2] Review |
| 46182f9f | implement | done | claude-opus-4-6 | [Workflow C2] Implement |
| abc7bc6a | review | done | codex | [Workflow C3] Review |
| 22009ab6 | implement | done | claude-opus-4-6 | [Workflow C3] Implement |

## Failed Jobs (last 5 with details)
No failed jobs.

## Total Job Stats
- Total: 7
- Done: 7
- Failed: 0
- Cancelled: 0
- Success rate: 100%

## Git State
### Working tree status
```
(clean)
```

### Last 3 commits
```
8c42a77 test: add raw argv assertions for shell-safety regression coverage (M4)
007c36e fix: convert finalizeWorkflow() retry/fallback calls to execFileSync argv arrays
3a62e94 fix: convert pushAndCreatePr() shell calls to execFileSync argv arrays
```

## Latest Worklog Entry
```
## Cycle 3 — M4: Update focused regression tests for raw argv assertions and shell metacharacters
**Owner:** Implementer
**Timestamp:** 2026-04-09T03:05:00Z

### What changed
- `src/test/workflow-partial-pr.test.ts`: Added `execFileSyncCalls` tracking array that records raw `{ file, args, opts }` from every `execFileSync` call. Updated mock to populate this array before delegating to `execSync`. Added `execFileSyncCalls.length = 0` to all `beforeEach` blocks. Added import of `execFileSync` from `child_process`. Added new `describe('argv-safety regression: raw execFileSync assertions (M4)')` block with 4 focused test cases:
  1. Shell metacharacters (`backticks`, `$()`, `$USER`) in workflow title preserved as discrete argv elements to `gh pr create`
  2. `pushAndCreatePr()` "already exists" recovery uses argv-based `gh pr view`
  3. `finalizeWorkflow()` retry-loop uses argv-based `git push`
  4. `finalizeWorkflow()` post-retry fallback uses argv-based `gh pr view`

### Commits
- `8c42a77` test: add raw argv assertions for shell-safety regression coverage (M4)

### Test results
- workflow-partial-pr.test.ts: 35 passed, 0 failed
- Full suite: 866 passed, 0 failed (65 test files; 1 flaky `socket hang up` in batchTemplates.test.ts passes on re-run)
- TypeScript compilation: clean (no errors)

### Blockers
- None

### Next step
All milestones complete. Ready for final review and PR creation.
```

## Plan (truncated)
```
# Plan

## Goal
Fix PR creation failures caused by shell-interpreted workflow titles/bodies in `WorkflowManager.ts` without broadening the change into unrelated worktree and branch-management code.

## Assessment

**Cycle 3 review outcome:** The production changes from cycles 1-2 are sound for this Sentry issue. `pushAndCreatePr()` and the `finalizeWorkflow()` retry/fallback path now pass workflow-controlled values (`title`, `body`, `worktree_branch`) to `git`/`gh` via `execFileSync(..., argv)` rather than shell strings, which addresses the breadcrumbed `/bin/sh -c` parse failure.

**Verified during review:**
- The cycle commits `3a62e94` and `007c36e` exist in the hurlicane repository object database and contain the expected argv-based refactor in `WorkflowManager.ts`.
- The authoritative checkout at `/Users/barneyhussey-yeo/GitHub/personal/hurlicane` is still on `main` with unrelated local changes, so the exact cycle implementation had to be reviewed from the commit objects rather than from the working tree.
- The targeted workflow PR suite in the authoritative checkout passed: `npm test -- --run src/test/workflow-partial-pr.test.ts` → `31 passed`.

**Remaining gap:** Closed in cycle 3 — M4 adds `execFileSyncCalls` tracking and focused assertions that would fail if production code regressed to shell-string execution.

## Milestones
- [x] **M1: Convert `pushAndCreatePr()` PR publication calls to `execFileSync`** [M]
- [x] **M2: Convert `finalizeWorkflow()` retry/fallback publication calls to `execFileSync`** [S]
- [x] **M4: Update focused regression tests for raw argv assertions and shell metacharacters** [M] — Rewritten `child_process` mock records raw `execFileSync(file, args, opts)` calls. New focused test cases assert on `file` plus discrete `args` entries, with shell-metacharacter title (`backticks, $(), $USER`), `already exists` recovery, retry-loop push, and post-retry fallback — all would fail if code regressed to shell-string execution.

## Priority Order
All milestones complete.

## Risks
- Resolved: False-positive test coverage gap closed by raw argv assertions in M4.
- Resolved: Metacharacter regression case now explicitly tested with backticks, `$()`, and `$USER`.
```
