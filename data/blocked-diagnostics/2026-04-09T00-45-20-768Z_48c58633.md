# Workflow Blocked Diagnostic

## Summary
- **Title:** Sentry fix [hurlicane]: WorkflowBlocked: Workflow blocked: Sentry fix [hurlicane]: WorkflowBlocked: Work
- **ID:** 48c58633-7c1f-472e-91cf-0a0ddb694b8b
- **Blocked at:** 2026-04-09T00:45:20.793Z
- **Reason:** PR creation failed — worktree preserved for retry at /Users/barneyhussey-yeo/GitHub/personal/.orchestrator-worktrees/hurlicane/wf-48c58633
- **Phase:** idle
- **Cycle:** 4/5
- **Milestones:** 4/4
- **Implementer model:** claude-opus-4-6
- **Reviewer model:** codex
- **Worktree:** /Users/barneyhussey-yeo/GitHub/personal/.orchestrator-worktrees/hurlicane/wf-48c58633 (branch: workflow/sentry-fix-hurlicane-workflowblocked-wor-48c58633)

## Job History (last 10)
| ID | Phase | Status | Model | Title |
|----|-------|--------|-------|-------|
| 7a7b39df | assess | done | claude-opus-4-6 | [Workflow C0] Assess |
| 94662232 | assess | done | claude-opus-4-6 | [Workflow C0] Assess quick repair |
| 6f45bd98 | review | done | codex | [Workflow C1] Review |
| a0b15782 | implement | done | claude-opus-4-6 | [Workflow C1] Implement |
| dc51e358 | review | done | codex | [Workflow C2] Review |
| c4a6a2cc | implement | done | claude-opus-4-6 | [Workflow C2] Implement |
| 4e73bec2 | review | done | codex | [Workflow C3] Review |
| 93dacb39 | implement | done | claude-opus-4-6 | [Workflow C3] Implement |
| b10f5f67 | review | done | codex | [Workflow C4] Review |
| b7802c01 | implement | done | claude-opus-4-6 | [Workflow C4] Implement |

## Failed Jobs (last 5 with details)
No failed jobs.

## Total Job Stats
- Total: 10
- Done: 10
- Failed: 0
- Cancelled: 0
- Success rate: 100%

## Git State
### Working tree status
```
(clean)
```

### Last 3 commits
```
51f5086 fix: refactor _removeWorktree to execFileSync for shell-safe worktree cleanup
ca0980e fix: refactor finalize/count helpers to execFileSync for shell-safe execution
b84ed57 fix: refactor pushAndCreatePr to execFileSync for shell-safe PR creation
```

## Latest Worklog Entry
```
## Cycle 4 — M4: Refactor _removeWorktree to execFileSync and finish targeted verification
**Owner:** Implementer
**Timestamp:** 2026-04-09T01:44:00Z

### What changed
- `src/server/orchestrator/WorkflowManager.ts`: Converted all 6 `execSync` calls in `_removeWorktree()` to `execFileSync` argument arrays:
  1. `git status --porcelain` — check for uncommitted work
  2. `git add -A` — stage uncommitted changes
  3. `git commit -m "wip: ..."` — save uncommitted work
  4. `git push origin <branch>` — best-effort push (branch no longer passed through JSON.stringify/shell)
  5. `git worktree remove --force <path>` — worktree cleanup (path no longer shell-interpolated)
  6. `git worktree prune` — prune stale worktree entries
- `src/test/workflow-partial-pr.test.ts`: 
  - Added `git status --porcelain`, `git add`, `git commit` handlers to both factory `execFileSync` mock and `defaultExecFileSyncMock`
  - Removed `_removeWorktree`-specific handlers (`git push`, `git status --porcelain`, `git worktree remove`) from `execSync` mock overrides in tests (a), (b), (d), (e)
  - Updated 8 worktree removal assertions from `execSyncCalls.find(c => c.cmd.includes('git worktree remove'))` to `execFileSyncCalls.find(c => c.file === 'git' && c.args[0] === 'worktree' && c.args[1] === 'remove')`
  - Updated comments to reflect M4 scope changes

### Commits
- `51f5086` fix: refactor _removeWorktree to execFileSync for shell-safe worktree cleanup

### Test results
- workflow-partial-pr.test.ts: 35 passed, 0 failed
- Full suite: 866 passed, 0 failed
- TypeScript: compiles cleanly (npx tsc --noEmit)

### Blockers
- None

### Next step
All milestones complete. The entire PR creation / finalize / cleanup path (pushAndCreatePr, getRemoteDefaultBranch, countCommitsAgainstBaseRef, finalizeWorkflow retry/fallback, and _removeWorktree) now uses execFileSync argument arrays, eliminating shell metacharacter injection.
```

## Plan (truncated)
```
# Plan: Fix Sentry WorkflowBlocked — shell-safe PR creation in WorkflowManager

## Root Cause

`pushAndCreatePr()` in `WorkflowManager.ts` builds shell command strings with `execSync(...)` plus `JSON.stringify(...)`. `JSON.stringify` does not escape shell metacharacters such as backticks, so workflow titles / PR bodies containing markdown code spans are still parsed by `/bin/sh`, which matches the Sentry failure:

```
/bin/sh: -c: line 0: unexpected EOF while looking for matching ``'
```

The blocking production path is `finalizeWorkflow()` → `pushAndCreatePr()`, but the same shell-string pattern also exists in helper code that decides whether to retry, block, or clean up the worktree.

## Milestones

- [x] **M1: Add WorkflowManager regression coverage for shell metacharacters** — Extend `src/test/workflow-partial-pr.test.ts` (not `pr-creator.test.ts`, which only covers DB integration for the standalone PR creator) so it mocks `execFileSync` and proves `pushAndCreatePr()` still succeeds when the workflow title / PR body contain backticks and other shell metacharacters such as `$()`. Acceptance: the test asserts `gh pr create` / `gh pr view` are invoked via argument arrays rather than shell strings, and the title/body reach `gh` verbatim.
- [x] **M2: Refactor the main PR creation path in `pushAndCreatePr()` to `execFileSync`** — Replace shell-string execution for the initial `git push`, optional `gh label create`, merge-conflict precheck (`git merge-base`, `git merge-tree`), existing-PR lookup, and `gh pr create` with `execFileSync(command, args, options)`. Preserve current behavior for draft PRs, partial labels, merge-conflict warnings, and the `already exists` recovery path. Acceptance: `pushAndCreatePr()` contains no `execSync(...)` calls; the live metacharacter regression no longer allows the vulnerable shell-string path; and the 3 M2-target tests in `workflow-partial-pr.test.ts` are unskipped and passing.
- [x] **M3: Refactor finalize/count helpers that control retry vs blocked outcomes** — Convert `getRemoteDefaultBranch()`, `countCommitsAgainstBaseRef()`, the retry `git push` inside `finalizeWorkflow()`, and the final `gh pr view` fallback to `execFileSync`. Replace shell-only constructs like `2>/dev/null || echo ""` and interpolated `^${baseRef}` with direct argv usage plus equivalent try/catch handling. Update `src/test/workflow-partial-pr.test.ts` so the finalize/retry/fallback and count-helper assertions stop keying off `execSync` shell strings and instead prove these paths use `execFileSync` argument arrays. Acceptance: missing remote refs still return `null`, transient `rev-parse`/`rev-list` failures still propagate or safe-default exactly as today, the finalize fallback still finds an existing PR, and targeted tests fail if these M3 paths regress back to shell strings.
- [x] **M4: Land the shell-safe refactor in the source-of-truth checkout and finish targeted verification** — In `/Users/barneyhussey-yeo/GitHub/personal/hurlicane`, `
```
