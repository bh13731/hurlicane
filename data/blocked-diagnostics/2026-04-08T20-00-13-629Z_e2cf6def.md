# Workflow Blocked Diagnostic

## Summary
- **Title:** Autonomous Agent Run: ## Error Boundaries & Route Resilience — Get to 10
- **ID:** e2cf6def-311d-44db-870e-e3b73b95b9e0
- **Blocked at:** 2026-04-08T20:00:13.629Z
- **Reason:** Worktree branch verification failed before implement: spawnSync /bin/sh ENOENT
- **Phase:** implement
- **Cycle:** 1/3
- **Milestones:** 0/4
- **Implementer model:** claude-sonnet-4-6
- **Reviewer model:** codex
- **Worktree:** /Users/barneyhussey-yeo/GitHub/personal/.orchestrator-worktrees/ceo-dashboard/wf-e2cf6def (branch: workflow/autonomous-agent-run-error-boundaries-ro-e2cf6def)

## Job History (last 10)
| ID | Phase | Status | Model | Title |
|----|-------|--------|-------|-------|
| 6503dae9 | assess | done | codex | [Workflow C0] Assess |
| 82c5923a | review | done | codex | [Workflow C1] Review |

## Failed Jobs (last 5 with details)
No failed jobs.

## Total Job Stats
- Total: 2
- Done: 2
- Failed: 0
- Cancelled: 0
- Success rate: 100%

## Git State
(git state unavailable)

## Latest Worklog Entry
```
(no worklog found)
```

## Plan (truncated)
```
# Plan

## Goal
Raise dashboard resilience to 10/10 by replacing silent route failures with proper Next.js error boundaries, bounding Clerk auth latency, and ensuring sync APIs always return controlled JSON errors.

## Assessment
The main implementation risks are now concrete. Dashboard data failures are still being swallowed in `src/app/dashboard/page.tsx`, `src/app/dashboard/unit-economics/page.tsx`, `src/app/dashboard/product/page.tsx`, `src/app/dashboard/people/page.tsx`, `src/app/dashboard/okrs/page.tsx`, and `src/app/dashboard/financial/page.tsx`, so simply adding `error.tsx` files would not make those routes resilient. Clerk auth is still unbounded in both `src/lib/auth/roles.server.ts` and `src/lib/sync/request-auth.ts`, and there are currently no tests for `roles.server.ts`, so the timeout behavior for page/layout auth is not pinned down. On the fallback side, the app has `src/app/global-error.tsx`, but it still renders `next/error`; the requested root fallback actually needs both a styled `src/app/error.tsx` for route-segment crashes and a styled `src/app/global-error.tsx` for root-layout failures. The sync routes `/api/cron`, `/api/sync/slack`, `/api/sync/mode`, and `/api/sync/management-accounts` also still allow uncaught exceptions from auth, enqueueing, and worker startup. Verification remains blocked in this worktree until dependencies are present: `make test`, `npx tsc --noEmit`, and `npx eslint src/` cannot run successfully without restoring local installs first.

## Milestones
- [ ] **M1: Bounded Clerk auth path and callsite audit** [M] — Add a shared 5s-bounded Clerk lookup primitive and route all current auth entry points through it. Acceptance: `src/lib/auth/roles.server.ts` no longer awaits `currentUser()` directly and safely redirects/falls back on timeout for dashboard/layout usage; `src/lib/sync/request-auth.ts` uses the same bounded lookup and treats timeout as an unauthenticated auth failure rather than hanging; no direct `currentUser()` calls remain outside the shared helper; tests cover normal success, unauthenticated, and timeout behavior for both sync auth and server role resolution.
- [ ] **M2: Root-level Paper Folio error fallbacks** [M] — Introduce a shared design-system-aligned error state with retry affordance, then wire it into the app-level boundaries. Acceptance: add `src/app/error.tsx` for route-level crashes under the root layout; replace the default `NextError` UI in `src/app/global-error.tsx` with the same Paper Folio fallback while preserving Sentry capture; both boundaries expose retry/reset behavior and use the requested styling primitives (`font-display`, `shadow-warm`, `bg-background`).
- [ ] **M3: Dashboard boundaries plus unsuppressed fatal loads** [L] — Add dashboard-segment `error.tsx` coverage and remove only the failure-swallowing patterns that currently prevent boundaries from activating. Acceptance: `src/app/dashboard/error.tsx` catches dashboard page/layout failures while preserving dashboa
```
