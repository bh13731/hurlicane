# Workflow Blocked Diagnostic

## Summary
- **Title:** Autonomous Agent Run: ## Test Coverage & Fault Injection — Get to 10/10

- **ID:** 311c402f-c676-4ed2-922a-33cfbdb98b9f
- **Blocked at:** 2026-04-08T20:00:46.167Z
- **Reason:** Startup worktree health check failed: Worktree recreation failed: Command failed: git worktree add "/Users/barneyhussey-yeo/GitHub/personal/.orchestrator-worktrees/ceo-dashboard/wf-311c402f" "workflow/autonomous-agent-run-test-coverage-fault-311c402f"
fatal: invalid reference: workflow/autonomous-agent-run-test-coverage-fault-311c402f

- **Phase:** implement
- **Cycle:** 1/3
- **Milestones:** 0/4
- **Implementer model:** claude-sonnet-4-6
- **Reviewer model:** codex
- **Worktree:** /Users/barneyhussey-yeo/GitHub/personal/.orchestrator-worktrees/ceo-dashboard/wf-311c402f (branch: workflow/autonomous-agent-run-test-coverage-fault-311c402f)

## Job History (last 10)
| ID | Phase | Status | Model | Title |
|----|-------|--------|-------|-------|
| 099659bd | assess | done | codex | [Workflow C0] Assess |
| 24b2ad56 | review | done | codex | [Workflow C1] Review |
| de8a19d0 | implement | queued | claude-sonnet-4-6 | [Workflow C1] Implement |

## Failed Jobs (last 5 with details)
No failed jobs.

## Total Job Stats
- Total: 3
- Done: 2
- Failed: 0
- Cancelled: 0
- Success rate: 67%

## Git State
(git state unavailable)

## Latest Worklog Entry
```
(no worklog found)
```

## Plan (truncated)
```
# Plan

## Goal
Raise the dashboard test suite to cover sync fault-injection, dashboard page empty/partial-data rendering, and chart edge cases, then get the full Vitest suite passing reliably.

## Assessment
The worktree currently has roughly 111 individual test cases, with good unit coverage around auth, data helpers, sync coordination helpers, and some transport-level resilience. The major gaps are at the boundaries this task targets: there are no dashboard page render tests under `src/app/dashboard`, no chart component tests under `src/components/charts`, and no sync-runner tests that verify graceful degradation when DB access fails before a sync loop begins, Slack returns a 429 while syncing a channel, Mode query fetches time out, or the LLM parser returns malformed JSON from within Slack sync. The page code already contains several explicit fallback branches (`.catch(() => fallback)`, empty-state cards, and `redirect("/dashboard")` guards), so the missing work is precise harnessing and assertions rather than broad product refactors. One immediate blocker is environmental: `npx vitest run` fails before loading the suite because this worktree has no local `node_modules`, so `vitest/config` and `@vitejs/plugin-react` cannot be resolved; the first implementation step must make test execution reproducible here before any milestone can be verified.

## Milestones
- [ ] **M1: Restore reproducible test execution in this worktree** [S] — Fix the local test environment so the worktree can run the repo’s Vitest config and React plugin without unresolved-import startup failures. Acceptance criteria: `npx vitest run` starts using repo-local dependencies from this worktree, the pre-change suite reaches normal test discovery/execution, and the implementer records the baseline pass/fail state before adding new tests.
- [ ] **M2: Add sync-runner fault-injection coverage for structured failure contracts** [L] — Add targeted tests next to the sync modules for the actual top-level entrypoints (`runSlackSync`, `runModeSync`, and `runManagementAccountsSync`) rather than only lower-level helpers. Cover four failure classes: (1) Postgres unavailable during sync preflight or persistence paths, proving the runner returns a structured `{ status: "error"|"partial", recordsSynced, errors }` result instead of throwing; (2) Slack 429/rate-limit failures while syncing one configured channel, proving channel-level failure is collected in `errors` and other channels can still complete; (3) Mode query timeout failures during report sync, proving the failed query/report is recorded while the run continues and returns `partial` when any report succeeds; (4) malformed LLM JSON while parsing an OKR update, proving the Slack sync run degrades to a structured failure result rather than crashing the worker. Acceptance criteria: no test calls real services, mocks are scoped to DB/API boundaries, and assertions check the exact returned status/error aggregation semantics for each runn
```
