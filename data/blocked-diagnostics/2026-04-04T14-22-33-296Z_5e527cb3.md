# Workflow Blocked Diagnostic

## Summary
- **Title:** Fix recovery ledger race conditions with atomic ops
- **ID:** 5e527cb3-9422-438b-8a81-d863df3c9d1b
- **Blocked at:** 2026-04-04T14:22:33.332Z
- **Reason:** PR creation failed — worktree preserved for retry at /Users/barneyhussey-yeo/GitHub/personal/.orchestrator-worktrees/hurlicane/wf-5e527cb3
- **Phase:** idle
- **Cycle:** 1/10
- **Milestones:** 4/4
- **Implementer model:** claude-sonnet-4-6
- **Reviewer model:** codex-gpt-5.4
- **Worktree:** /Users/barneyhussey-yeo/GitHub/personal/.orchestrator-worktrees/hurlicane/wf-5e527cb3 (branch: workflow/fix-recovery-ledger-race-conditions-with-5e527cb3)

## Job History (last 10)
| ID | Phase | Status | Model | Title |
|----|-------|--------|-------|-------|
| 644b0c62 | assess | done | claude-sonnet-4-6 | [Workflow C0] Assess |
| 058945c8 | assess | done | claude-sonnet-4-6 | [Workflow C0] Assess quick repair |
| a8a8a7ad | assess | done | claude-sonnet-4-6 | [Workflow C0] Assess diagnostic repair |
| 3ebeb034 | assess | done | claude-sonnet-4-6 | [Workflow C0] Assess full re-assess repair |
| 61960361 | assess | done | claude-sonnet-4-6 | [Workflow C0] Assess (resumed) |
| 3e31204c | assess | done | claude-sonnet-4-6 | [Workflow C0] Assess (resumed) |
| 146e9fe2 | assess | done | claude-sonnet-4-6 | [Workflow C0] Assess (resumed) |
| ae4854db | assess | done | claude-sonnet-4-6 | [Workflow C0] Assess (resumed) |
| ce79245f | review | done | codex-gpt-5.4 | [Workflow C1] Review |
| c65c085d | implement | done | claude-sonnet-4-6 | [Workflow C1] Implement |

## Failed Jobs (last 5 with details)
No failed jobs.

## Total Job Stats
- Total: 10
- Done: 10
- Failed: 0
- Cancelled: 0
- Success rate: 100%

## Git State
### Working tree status
```
?? 0
?? node_modules
```

### Last 3 commits
```
d9e1d57 test: add same-model CLI retry idempotency regression coverage (M3)
2412579 fix: replace TOCTOU getNote+upsertNote with atomic insertNoteIfNotExists
ddbd3ed feat: add insertNoteIfNotExists to queries.ts for atomic idempotency
```

## Latest Worklog Entry
```
## Cycle 1 — All milestones (M1–M4)
**Owner:** Implementer
**Timestamp:** 2026-04-04T15:22:00Z

### What changed
- `src/server/db/queries.ts`: Added `insertNoteIfNotExists(key, value, agentId)` after `upsertNote`. Uses `INSERT OR IGNORE` on the notes PRIMARY KEY; returns `result.changes > 0` to identify the winner of a concurrent insert race.
- `src/server/orchestrator/WorkflowManager.ts`: Replaced all three TOCTOU check-then-insert patterns in `_onJobCompleted` recovery paths with single atomic `insertNoteIfNotExists` calls:
  1. model-fallback branch: `getNote(recoveryKey)` + `upsertNote(recoveryKey, ...)` → `!insertNoteIfNotExists(recoveryKey, ...)`
  2. same-model CLI retry branch: `getNote(cliRetryKey)` + `upsertNote(cliRetryKey, ...)` → `!insertNoteIfNotExists(cliRetryKey, ...)`; `attemptsKey` upsert moved inside the winner path only
  3. alt-provider branch: `getNote(altProviderKey)` + `upsertNote(altProviderKey, ...)` → `!insertNoteIfNotExists(altProviderKey, ...)`
- `src/test/workflow-dedup.test.ts`: Added "codex_cli_crash duplicate onJobCompleted spawns exactly one retry and increments counter once" regression test directly covering the CLI retry TOCTOU path.

### Commits
- `ddbd3ed` feat: add insertNoteIfNotExists to queries.ts for atomic idempotency
- `2412579` fix: replace TOCTOU getNote+upsertNote with atomic insertNoteIfNotExists
- `d9e1d57` test: add same-model CLI retry idempotency regression coverage (M3)

### Test results
- Full suite: 828 passed, 0 failed (61 test files)

### Blockers
- None. The worktree had no `node_modules`; symlinked from the main repo to run tests.

### Next step
All milestones complete. Ready for reviewer cycle.

```

## Plan (truncated)
```
# Plan

## Goal
Add `insertNoteIfNotExists` to `queries.ts` and use it to atomically eliminate TOCTOU race conditions in `WorkflowManager.ts` recovery paths.

## Assessment
`queries.ts` already has the right pattern in `insertAgentOutput`: use `INSERT OR IGNORE`, inspect `result.changes`, and only treat the caller as the winner when a row was actually inserted. The `notes` table already supports this because `key TEXT PRIMARY KEY` enforces uniqueness.

The current plan is correct in direction but needed two improvements:
1. It did not require direct regression coverage for the same-model CLI retry branch, even though that branch is one of the three call sites being changed. Existing recovery tests already cover model-fallback and alt-provider dedupe, but not the `attempts < MAX_CLI_RETRIES` path.
2. M2 was too loose about observable behavior. The implementation must preserve existing blocked/log behavior and ensure `attemptsKey` is only incremented by the winner after the atomic insert succeeds.

## Milestones
- [x] **M1: Add `insertNoteIfNotExists` to `queries.ts`** [S] — Add `export function insertNoteIfNotExists(key: string, value: string, agentId: string | null): boolean` next to the existing note helpers. Implement it with `INSERT OR IGNORE INTO notes (key, value, agent_id, updated_at) VALUES (?, ?, ?, ?)` and return `result.changes > 0`. No schema or migration changes.

- [x] **M2: Replace all three recovery TOCTOU patterns in `WorkflowManager.ts`** [S] — Update the three recovery idempotency branches in `_onJobCompleted` to gate spawning on `insertNoteIfNotExists(...)` instead of `getNote(...)` + `upsertNote(...)`:
  1. model-fallback recovery key
  2. same-model CLI retry key
  3. alt-provider recovery key
Acceptance criteria: duplicate completions still log/return early without spawning a second job; the CLI retry counter note (`attemptsKey`) is updated only inside the winning branch after the atomic insert succeeds; blocked reasons and existing fallback/no-fallback behavior remain unchanged.

- [x] **M3: Extend recovery regression coverage for the changed branches** [S] — Update the existing recovery-focused tests in `src/test/error-handling-recovery.test.ts` (or the nearest existing workflow recovery suite) so the edited behavior is directly exercised. At minimum, add coverage for the same-model retry branch where `attempts < MAX_CLI_RETRIES` and duplicate `onJobCompleted(..., { force: true })` calls must spawn exactly one retry and increment the attempts counter exactly once. Reuse the existing model-fallback and alt-provider idempotency tests as the pattern.

- [x] **M4: Run `npm test` and confirm the full suite passes** [S] — Run the full test suite after the code and regression test updates. If failures appear, fix them before marking the workflow milestone complete.

```
