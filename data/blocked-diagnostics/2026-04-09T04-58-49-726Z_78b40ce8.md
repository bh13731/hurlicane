# Workflow Blocked Diagnostic

## Summary
- **Title:** Sentry fix [hurlicane]: WorkflowBlocked: Workflow blocked: Sentry fix [hurlicane]: WorkflowBlocked: Work
- **ID:** 78b40ce8-171a-4225-ab31-6e51bbbea3f1
- **Blocked at:** 2026-04-09T04:58:49.758Z
- **Reason:** Reached max cycles (5) with 2/4 milestones complete
- **Phase:** idle
- **Cycle:** 5/5
- **Milestones:** 2/4
- **Implementer model:** claude-opus-4-6
- **Reviewer model:** codex
- **Worktree:** /Users/barneyhussey-yeo/GitHub/personal/.orchestrator-worktrees/hurlicane/wf-78b40ce8 (branch: workflow/sentry-fix-hurlicane-workflowblocked-wor-78b40ce8)

## Job History (last 10)
| ID | Phase | Status | Model | Title |
|----|-------|--------|-------|-------|
| ff81edde | implement | done | claude-sonnet-4-6[1m] | [Workflow C2] Implement (fallback) |
| b43da15c | review | done | codex | [Workflow C3] Review |
| 2dd8b372 | implement | done | claude-opus-4-6 | [Workflow C3] Implement |
| 0c833809 | review | done | codex | [Workflow C3] Review |
| 3173b594 | implement | done | claude-opus-4-6 | [Workflow C3] Implement |
| d023ebdd | review | done | codex | [Workflow C4] Review |
| 1811ec3f | implement | failed | claude-opus-4-6 | [Workflow C4] Implement |
| bc5e13cd | implement | done | codex | [Workflow C4] Implement (fallback) |
| 1e8cecfd | review | done | codex | [Workflow C5] Review |
| beb08566 | implement | done | claude-opus-4-6 | [Workflow C5] Implement |

## Failed Jobs (last 5 with details)
### [Workflow C2] Implement
- **Job ID:** 482ccabf | **Agent ID:** 3dac9c5a
- **Phase:** implement | **Model:** claude-opus-4-6
- **Exit code:** n/a | **Turns used:** n/a | **Cost:** $n/a
- **DB Error:**
```
PTY attach failed after 4 attempts: posix_spawnp failed.
```
- **Agent output (last lines):**
```
ERATIONAL_BLOCK_PATTERNS`) is already in the working tree. The `Draft PR creation failed` pattern isn't needed in `WorkflowManager.ts` because that path goes through `queries.updateWorkflow` directly in the API layer, not through `updateAndEmit`.

What M1 needs is: test coverage proving the finalize-path PR failure blocks **without** Sentry capture, and a counterpart proving non-operational blocks **still** capture to Sentry.

Let me check if `updateAndEmit` is exported, and look at the helpers.
Now let me check what test helpers are available.
Now I have all the context. Let me lock the file and make the changes.
Good. Now let me add the Sentry gating tests. First, I'll add `insertTestProject` and `insertTestJob` to the imports, and then add the new describe block.
Now add the new describe block at the end of the file (after line 1175, the end of the last describe block):
Now let me also verify that the `'Draft PR creation failed'` line is not needed in `OPERATIONAL_BLOCK_PATTERNS` (since that path goes through `queries.updateWorkflow` not `updateAndEmit`). Let me also verify the working tree state is correct.
```

### [Workflow C4] Implement
- **Job ID:** 1811ec3f | **Agent ID:** 3f592f7c
- **Phase:** implement | **Model:** claude-opus-4-6
- **Exit code:** n/a | **Turns used:** n/a | **Cost:** $1.00
- **DB Error:**
```
API Error: 429 {"type":"error","error":{"type":"rate_limit_error","message":"This request would exceed your organization's rate limit of 30,000 input tokens per minute (org: 62dc307d-268c-4e85-b5a6-86a2613aab61, model: claude-opus-4-6). For details, refer to: https://docs.claude.com/en/api/rate-limits. You can see the response headers for current usage. Please reduce the prompt length or the maximum tokens requested, or try again later. You may also contact sales at https://claude.com/contact-sales to discuss your options for a rate limit increase."},"request_id":"req_011CZsbYVZXzjRAzSLV3kBpY"}
```
- **Agent output (last lines):**
```
Now update the `pushAndCreatePr: partial label` tests. I need to clear `execFileSyncCalls` in `beforeEach` and rewrite all assertions:
Now update the `finalizeWorkflow: worktree preservation on PR failure` tests. These call `finalizeWorkflow` which calls `pushAndCreatePr` (now using `execFileSync`):
[ERROR] unknown
Now update the `finalizeWorkflow: blocked status on PR failure` tests:
Now update `pushAndCreatePr: rev-list error handling` tests:
Now update the `Fix-C11a` tests. Let me re-read them to identify which ones call `pushAndCreatePr`:
Now update the `finalizeWorkflow: retry and fallback behavior` tests. These are the most complex ones:
Now I need to rewrite the retry/fallback tests. The key insight: `pushAndCreatePr` calls now go through `execFileSync`, but `finalizeWorkflow`'s own retry `git push` (line 1722) and fallback `gh pr view` (line 1734) still use `execSync` (M3 scope). I need to mock both:
[ERROR] unknown
[RESULT] "API Error: 429 {\"type\":\"error\",\"error\":{\"type\":\"rate_limit_error\",\"message\":\"This request would exceed your organization's rate limit of 30,000 input tokens per minute (org: 62dc307d-268c-4e85-b5a6-86a2613aab61, model: claude-opus-4-6). For details, refer to: https://docs.claude.com/en
```


## Total Job Stats
- Total: 19
- Done: 17
- Failed: 2
- Cancelled: 0
- Success rate: 89%

## Git State
### Working tree status
```
M src/server/orchestrator/WorkflowManager.ts
 M src/test/workflow-partial-pr.test.ts
```

### Last 3 commits
```
ae7224c fix(sentry): treat preserved-worktree PR failure blocks as operational
023a923 fix: fail fast when job work_dir does not exist (#40)
65e1aea test: cover PTY spawning-state cleanup
```

## Latest Worklog Entry
```
## Cycle 4 — M2: Finish the argv-safe `pushAndCreatePr` migration and repair the partial-PR test harness
**Owner:** Implementer
**Timestamp:** 2026-04-09T04:55:15Z

### What changed
- `src/test/workflow-partial-pr.test.ts`: Added a hoisted `child_process` harness that exports both `execSync` and `execFileSync`, records argv-based calls separately, and resets cleanly between tests.
- `src/test/workflow-partial-pr.test.ts`: Rewrote the `pushAndCreatePr` partial-PR assertions to validate argv arrays for `git push`, `gh label create`, `gh pr view`, and `gh pr create` instead of shell-string substrings.
- `src/test/workflow-partial-pr.test.ts`: Added regressions proving backticks and `$()` are passed literally in argv for both normal and draft partial PR creation, while preserving the existing conflict-warning, label-already-exists, existing-PR, and transient-git-error coverage.

### Commits
- `5544609` test(workflow): repair partial PR argv harness

### Test results
- `npm test -- src/test/workflow-partial-pr.test.ts`: 35 passed, 0 failed
- `npm test`: 872 passed, 0 failed
- `npx tsc --noEmit`: 0 failed

### Blockers
- None

### Next step
Implement M3 by converting `finalizeWorkflow` retry/fallback publication calls to argv-based `execFileSync` and then updating the finalize/reconcile assertions in `src/test/workflow-partial-pr.test.ts` to inspect argv calls directly.
```

## Plan (truncated)
```
# Plan

## Goal
Fix the WorkflowBlocked Sentry noise from workflow finalization and the underlying PR-publication shell-argv bug in `/Users/barneyhussey-yeo/GitHub/personal/hurlicane` by moving workflow PR publication off shell strings and treating preserved-worktree PR retry blocks as operational, without suppressing real workflow failures.

## Review Findings

### Current cycle review
1. The source-of-truth checkout in `/Users/barneyhussey-yeo/GitHub/personal/hurlicane` is still on `main` and dirty; `git log --oneline -10` shows `5544609 test(workflow): repair partial PR argv harness` as the only local commit ahead of `origin/main`, while `src/server/orchestrator/WorkflowManager.ts` and `src/server/orchestrator/PtyManager.ts` also have unrelated uncommitted diffs present in the live checkout.
2. Cycle 4’s committed work in `src/test/workflow-partial-pr.test.ts` is good: the hoisted `child_process` harness now exports both `execSync` and `execFileSync`, records argv calls separately, and the `pushAndCreatePr` partial-PR assertions now validate argv arrays for `git push`, `gh label create`, `gh pr view`, and `gh pr create`, including the backtick/`$()` literal regressions for both normal and draft partial PRs.
3. Verification is green in the live checkout: `npm test` passes with 872 passing tests and `npx tsc --noEmit` passes cleanly.
4. M3 is still genuinely incomplete in the source-of-truth checkout: `finalizeWorkflow` still shells out through interpolated strings for the retry-loop `git push` and fallback `gh pr view` calls in `src/server/orchestrator/WorkflowManager.ts`, and the finalize/reconcile tests in `src/test/workflow-partial-pr.test.ts` still mock/assert those paths via `execSync` string commands instead of argv-based `execFileSync` calls.
5. Real follow-up bug: the live checkout’s operational allowlist in `src/server/orchestrator/WorkflowManager.ts` contains `PR creation failed` but not `Draft PR creation failed`, while the draft wrap-up path in `src/server/api/workflows.ts` still blocks workflows with `Draft PR creation failed — worktree preserved for retry at ...`; that draft-preserved path would therefore still report to Sentry in the source-of-truth checkout.

## Milestones
- [x] **M1: Treat preserved-worktree PR creation blocks as operational in `WorkflowManager`** [S] — Keep the operational allowlist narrowly scoped to preserved-worktree PR publication failures, and add WorkflowManager-level regressions that prove `finalizeWorkflow` transitions to `blocked` with `PR creation failed — worktree preserved for retry ...` without calling `Sentry.captureException`, while a non-operational blocked transition still does call Sentry. Reuse the existing `workflow-worktree-guard` Sentry assertion pattern instead of only mocking Sentry in `workflow-partial-pr.test.ts`. Keep the existing API wrap-up regression proving the draft path still returns `draft_pr_failed_preserved` with `Draft PR creation failed ...` and preserves the worktree. Acc
```
