# Workflow Blocked Diagnostic

## Summary
- **Title:** Fix: Always use worktrees + always raise PR
- **ID:** 02f7c65a-48e0-4e79-bc79-1f756f35d9b1
- **Blocked at:** 2026-04-03T13:49:55.202Z
- **Reason:** Reached max cycles (4) with 6/9 milestones complete
- **Phase:** idle
- **Cycle:** 4/4
- **Milestones:** 6/9
- **Implementer model:** claude-opus-4-6
- **Reviewer model:** claude-opus-4-6
- **Worktree:** none (branch: none)

## Job History (last 10)
| ID | Phase | Status | Model | Title |
|----|-------|--------|-------|-------|
| 587236fd | assess | done | claude-sonnet-4-6[1m] | [Workflow C0] Assess |
| f42b4bc1 | review | done | claude-opus-4-6 | [Workflow C1] Review |
| e12e5a6e | implement | failed | claude-opus-4-6 | [Workflow C1] Implement |
| c2ce71e3 | implement | done | claude-opus-4-6 | [Workflow C1] Implement (resumed) |
| 80389f69 | review | done | claude-opus-4-6 | [Workflow C2] Review |
| bd1d0208 | implement | done | claude-opus-4-6 | [Workflow C2] Implement (resumed) |
| 11202d35 | review | done | claude-opus-4-6 | [Workflow C3] Review |
| abdfc659 | implement | done | codex | [Workflow C3] Implement |
| e0b07061 | review | done | codex | [Workflow C4] Review |
| 516adaef | implement | done | codex | [Workflow C4] Implement |

## Failed Jobs (last 5 with details)
### [Workflow C1] Implement
- **Job ID:** e12e5a6e | **Agent ID:** 9df0c00b
- **Phase:** implement | **Model:** claude-opus-4-6
- **Exit code:** n/a | **Turns used:** n/a | **Cost:** $n/a
- **DB Error:**
```
Command failed: tmux new-session -d -s orchestrator-9df0c00b-8f19-4bdd-bbd6-0d8c034a7165 -x 100 -y 50 /Users/barneyhussey-yeo/GitHub/personal/hurlicane/data/agent-scripts/9df0c00b-8f19-4bdd-bbd6-0d8c034a7165.sh
create window failed: fork failed: Device not configured

```
- **Agent output (last lines):**
```
(no log output available)
```


## Total Job Stats
- Total: 10
- Done: 9
- Failed: 1
- Cancelled: 0
- Success rate: 90%

## Git State
(no worktree configured)

## Latest Worklog Entry
```
## Cycle 4 — M2: Fix startWorkflow worktree-failure fallback
**Owner:** Implementer
**Timestamp:** 2026-04-03T13:49:05Z

### What changed
- `src/server/orchestrator/WorkflowManager.ts`: Changed `startWorkflow` so a `git worktree add` failure blocks the workflow with a clear `blocked_reason` and returns `null` before any assess job is created.
- `src/test/workflow-preflight.test.ts`: Added a regression test covering `use_worktree=1` plus `git worktree add` failure, asserting the workflow is blocked and no assess job is inserted.

### Commits
- `cd481a6` fix: block workflow start on worktree creation failure

### Test results
- `npm test -- src/test/workflow-preflight.test.ts`: 4 passed, 0 failed
- `npm test`: 603 passed, 0 failed

### Blockers
- None

### Next step
M3: recreate missing workflow worktrees during `resumeWorkflow` before any status transition or phase job spawn.

```

## Plan (truncated)
```
# Plan

## Goal
Fix two critical bugs in hurlicane's workflow system: worktrees not always created when required, and completed workflows not always generating PRs.

## Assessment
Review of the current checkout shows the core acceptance criteria are still not implemented even though the test suite is green.

Concrete findings from code review:

- **Issue 1: `startWorkflow` still silently falls back to `work_dir` when worktree creation fails.** In `WorkflowManager.ts:919-942`, the `git worktree add` failure path only logs a warning and then continues creating the assess job with `work_dir`, which can still put workflow commits on `main`.
- **Issue 2: `resumeWorkflow` still does not recreate a missing worktree and flips status to `running` too early.** In `WorkflowManager.ts:993-1010`, resume only health-checks an existing worktree; if `use_worktree=1` and `worktree_path` is null, it skips creation entirely and immediately sets `status='running'`, so the resumed job will still use `work_dir` directly.
- **Issue 3: PR creation still hard-depends on `worktree_path`, so completed and partial workflows can still finish without a PR.** `pushAndCreatePr` returns null when `worktree_path` is null (`WorkflowManager.ts:1103-1106`), `finalizeWorkflow` returns early on the same condition (`WorkflowManager.ts:1172-1179`), and the max-cycles partial-PR path at `WorkflowManager.ts:253-258` still calls this function, so the Architecture & Design failure mode is still present.
- **Issue 4: the current plan still does not cover the required fallback for workflows that already committed onto `main`.** The task requires creating a new branch and cherry-picking workflow commits when completion happens without a worktree; there is no implementation or test coverage for that recovery path yet.

## Milestones

- [x] **M1: Add spawnPhaseJob safety guard — block when use_worktree=1 but worktree_path is null** — Completed in cycle 1. Guard at line 702 blocks with diagnostic reason. Tests in `workflow-worktree-guard.test.ts`.

- [x] **Fix-C2a: Add missing instrument.js mock in workflow-worktree-guard.test.ts** — Completed in cycle 2. Added `vi.mock('../server/instrument.js', ...)` matching slow-progress.test.ts pattern.

- [x] **Fix-C2b: Scope Sentry captureException to error-level blocks only** — Completed in cycle 2. Added `OPERATIONAL_BLOCK_PATTERNS` allowlist in `updateAndEmit` to skip Sentry for max-cycles, zero-progress, and diminishing-returns blocks. Two new tests verify Sentry fires for error blocks but not operational blocks.

- [x] **Fix-C3a: Fix writeBlockedDiagnostic test-env guard that breaks its own tests** — Verified in cycle 3. `workflow-blocked-diagnostic.test.ts` already passes in the current checkout, so no additional code change was required this cycle.

- [x] **Fix-C3b: Fix re-plan logic breaking zero-progress and diminishing returns tests** — Completed in cycle 3. Seeded `workflow/{id}/replan-attempted/{cycle}` in the four affected test setups so they 
```
