# Workflow Blocked Diagnostic

## Summary
- **Title:** Sentry fix [hurlicane]: WorkflowBlocked: Workflow blocked: Autonomous Agent Run: ## Code Quality & Consi
- **ID:** a9b9d5ce-c52e-44db-a428-499a1aaef35b
- **Blocked at:** 2026-04-08T22:09:11.866Z
- **Reason:** PR creation failed — worktree preserved for retry at /Users/barneyhussey-yeo/GitHub/personal/.orchestrator-worktrees/hurlicane/wf-a9b9d5ce
- **Phase:** idle
- **Cycle:** 4/5
- **Milestones:** 3/3
- **Implementer model:** claude-opus-4-6
- **Reviewer model:** codex
- **Worktree:** /Users/barneyhussey-yeo/GitHub/personal/.orchestrator-worktrees/hurlicane/wf-a9b9d5ce (branch: workflow/sentry-fix-hurlicane-workflowblocked-wor-a9b9d5ce)

## Job History (last 10)
| ID | Phase | Status | Model | Title |
|----|-------|--------|-------|-------|
| d032d7ed | implement | failed | claude-sonnet-4-6 | [Workflow C1] Implement |
| ff19991c | implement | done | codex | [Workflow C1] Implement (fallback) |
| 8f7c9968 | review | done | codex | [Workflow C2] Review |
| d8579dc5 | implement | done | codex | [Workflow C2] Implement |
| 1901f7ad | review | done | codex | [Workflow C3] Review |
| 55de6ede | implement | done | codex | [Workflow C3] Implement |
| eb9d4419 | review | done | codex | [Workflow C4] Review |
| 55c5be40 | implement | done | claude-opus-4-6 | [Workflow C4] Implement |
| 6900406b | review | done | claude-opus-4-6 | [Workflow C4] Review |
| 7572912f | implement | done | claude-sonnet-4-6[1m] | [Workflow C4] Implement |

## Failed Jobs (last 5 with details)
### [Workflow C1] Implement
- **Job ID:** d032d7ed | **Agent ID:** 4680c01d
- **Phase:** implement | **Model:** claude-sonnet-4-6
- **Exit code:** n/a | **Turns used:** n/a | **Cost:** $n/a
- **DB Error:**
```
PTY attach failed after 4 attempts: posix_spawnp failed.
```
- **Agent output (last lines):**
```
Good — I'm on the right branch in the worktree. The file I need to edit is in the worktree. Let me read it there:
Now let me look at the test file to understand the existing patterns before making changes:
Now let me look at the test file's beginning and the `pushAndCreatePr` / `countBranchCommits` mocking patterns:
Let me look at how `finalizeWorkflow` and `PrCreator` are used in WorkflowManager:
Let me look at the `onJobCompleted` function to understand when `finalizeWorkflow` is triggered:
```


## Total Job Stats
- Total: 13
- Done: 12
- Failed: 1
- Cancelled: 0
- Success rate: 92%

## Git State
### Working tree status
```
(clean)
```

### Last 3 commits
```
80566a5 fix: treat PR creation failure as operational block to suppress Sentry noise
023a923 fix: fail fast when job work_dir does not exist (#40)
65e1aea test: cover PTY spawning-state cleanup
```

## Latest Worklog Entry
```
## Cycle 4 — Add `'PR creation failed'` to `OPERATIONAL_BLOCK_PATTERNS`
**Owner:** Implementer
**Timestamp:** 2026-04-08T23:07:30Z

### What changed
- `src/server/orchestrator/WorkflowManager.ts`: Added `'PR creation failed'` to the `OPERATIONAL_BLOCK_PATTERNS` array in `updateAndEmit()`. This prevents the function from emitting a `WorkflowBlocked` Sentry exception when a workflow is blocked due to PR creation exhausting retries — matching the existing recovery behaviour in `reconcileBlockedPRs()`.

### Commits
- `80566a5` fix: treat PR creation failure as operational block to suppress Sentry noise

### Test results
- `npm run build`: succeeded (TypeScript + Vite)
- `npm test`: 862 passed, 0 failed (65 test files). One flaky socket hang-up in `localConfig.test.ts` appeared on first full-suite run but passed on re-run and isolated run — confirmed pre-existing flakiness unrelated to this change.

### Blockers
- None

### Next step
All milestones complete. Ready for reviewer to verify and create PR.

```

## Plan (truncated)
```
# Plan: Fix WorkflowBlocked Sentry Error for PR Creation Failures

## Problem
When PR creation fails after 3 retry attempts, `finalizeWorkflow` sets the workflow status to `blocked` with reason `PR creation failed — worktree preserved for retry at <path>`. `updateAndEmit()` still treats that reason as non-operational, so it emits a `WorkflowBlocked` Sentry exception even though `reconcileBlockedPRs()` already recognizes the same reason as a recoverable retry state.

## Root Cause
`OPERATIONAL_BLOCK_PATTERNS` in `src/server/orchestrator/WorkflowManager.ts` does not include the `PR creation failed` reason, even though the recovery path explicitly keys off that substring.

## Milestones

- [x] Investigate and confirm root cause in `WorkflowManager.ts` `updateAndEmit` function
- [x] Apply the production fix in `src/server/orchestrator/WorkflowManager.ts` by adding `'PR creation failed'` to `OPERATIONAL_BLOCK_PATTERNS`, and verify the task-scoped diff in `/Users/barneyhussey-yeo/GitHub/personal/hurlicane` touches only that array entry so PR-retry blocks are treated as operational while non-operational block reasons still report to Sentry.
- [x] Re-run `npm run build` and `npm test` after the code change and confirm the Sentry-noise fix lands without TypeScript or Vitest regressions.

## Files to Modify
- `src/server/orchestrator/WorkflowManager.ts` — extend `OPERATIONAL_BLOCK_PATTERNS` with `PR creation failed`

## Review Notes
- Cycle 4: Fix committed as `80566a5` on branch `workflow/sentry-fix-hurlicane-workflowblocked-wor-a9b9d5ce`. `npm run build` succeeds, `npm test` passes (862/862 tests). One flaky socket test in `localConfig.test.ts` appeared on first run but passed on re-run and in isolation — unrelated to the change.

```
