# Workflow Blocked Diagnostic

## Summary
- **Title:** Fix standalone --print job completion — detect ndjson result event
- **ID:** c6b64559-1ea0-458f-92b7-186d1f008746
- **Blocked at:** 2026-04-04T23:31:17.934Z
- **Reason:** 2 consecutive implement cycles with no milestone progress (0/4 complete)
- **Phase:** implement
- **Cycle:** 7/8
- **Milestones:** 0/4
- **Implementer model:** claude-sonnet-4-6
- **Reviewer model:** codex-gpt-5.4
- **Worktree:** /Users/barneyhussey-yeo/GitHub/personal/.orchestrator-worktrees/hurlicane/wf-c6b64559 (branch: workflow/fix-standalone-print-job-completion-dete-c6b64559)

## Job History (last 10)
| ID | Phase | Status | Model | Title |
|----|-------|--------|-------|-------|
| 531de4e8 | implement | done | claude-sonnet-4-6 | [Workflow C6] Implement |
| f535d3fb | review | done | codex-gpt-5.4 | [Workflow C6] Review |
| 61ad0f7b | implement | done | claude-sonnet-4-6 | [Workflow C6] Implement |
| e6b3847f | implement | done | claude-sonnet-4-6 | [Workflow C6] Implement (resumed) |
| 454abdd2 | review | done | codex-gpt-5.4 | [Workflow C6] Review |
| 4a317283 | implement | done | claude-sonnet-4-6 | [Workflow C6] Implement |
| 00939e7b | review | done | codex-gpt-5.4 | [Workflow C7] Review |
| 30950b4c | implement | done | claude-sonnet-4-6 | [Workflow C7] Implement |
| 776bc304 | review | done | codex-gpt-5.4 | [Workflow C7] Review |
| 57614f97 | implement | done | claude-sonnet-4-6 | [Workflow C7] Implement |

## Failed Jobs (last 5 with details)
No failed jobs.

## Total Job Stats
- Total: 60
- Done: 60
- Failed: 0
- Cancelled: 0
- Success rate: 100%

## Git State
### Working tree status
```
M src/server/orchestrator/PtyManager.ts
?? src/test/pty-manager.test.ts
```

### Last 3 commits
```
85c446d fix: atomic recovery ledger operations to prevent race conditions
f069bde refactor: audit and fix silent catch blocks across orchestrator
8b1c7f1 fix: use --print mode for all non-interactive Claude jobs
```

## Latest Worklog Entry
```
(no worklog found)
```

## Plan (truncated)
```
# Plan

## Goal
Fix standalone non-interactive (`--print` mode) jobs being incorrectly marked failed or done without validating their final ndjson outcome by making every standalone completion path resolve from the final ndjson `result` event first, then commit history only when no `result` event exists.

## Review Findings

1. **Cycle 7 review: `attachPty()` still has a standalone attach-failure path that skips the required `result -> commits -> fail` decision.** `src/server/orchestrator/PtyManager.ts:650-707` now fixes the fallback-poll branch, but when PTY attach retries exhaust and the tmux session is already gone, the `else if (!isAutoExitJob(job))` branch at `src/server/orchestrator/PtyManager.ts:702-706` still marks the agent/job failed directly from the PTY attach error. That path still bypasses the required standalone recovery contract and can misclassify a completed standalone print job whose final `result` event or commit evidence already exists.
2. **Cycle 7 review: `StuckJobWatchdog` still accepts forbidden turn events for standalone recovery.** `src/server/orchestrator/StuckJobWatchdog.ts:63-72` still returns terminal status from `turn.completed` and `turn.failed`, so dead-session standalone recovery can still succeed or fail without a final `result` event.
3. **Cycle 7 review: the `pty-manager` tests are still helper-level and still red.** `src/test/pty-manager.test.ts:138-239` still assert against `_statusFromNdjsonForTest()` rather than the production completion paths, and `npm test` on April 5, 2026 still fails in those assertions because the helper returns `null` instead of the expected terminal status.
4. **Cycle 7 review: watchdog regression coverage is still placeholder-only.** `src/test/stuck-job-watchdog.test.ts:27-46` are still empty stubs, so the touched dead-session recovery path remains effectively untested.

## Assessment

### Current Gap
The branch is still not shippable for this workflow:
- `attachPty()` still has an attach-failure branch for standalone jobs that skips the required `result -> commits -> fail` contract when tmux is already gone after PTY attach retries fail
- `StuckJobWatchdog.statusFromLog()` still treats `turn.completed` / `turn.failed` as standalone terminal evidence
- the `pty-manager` tests are still aimed at a private helper instead of the production attach / exit paths, and three of them are red
- the watchdog regression tests remain placeholder stubs
- `npm test` still fails

### Test Status
`npm test` failed on April 5, 2026 in `src/test/pty-manager.test.ts` with three failing assertions expecting `_statusFromNdjsonForTest()` to return `done` / `failed`, but receiving `null` instead.

## Milestones

- [ ] **M1: Apply the exact standalone `result -> commits -> fail` contract to every `attachPty()` exit path** [M] — In addition to the fallback-poll and main `onExit()` branches, fix the PTY-attach-failed + tmux-already-gone branch in `attachPty()` so standalone non-interactive, non-debate, non-
```
