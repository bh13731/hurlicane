# Workflow Blocked Diagnostic

## Summary
- **Title:** Fix standalone --print job completion — detect ndjson result event
- **ID:** c6b64559-1ea0-458f-92b7-186d1f008746
- **Blocked at:** 2026-04-04T23:41:27.154Z
- **Reason:** Reached max cycles (8) with 0/4 milestones complete
- **Phase:** idle
- **Cycle:** 8/8
- **Milestones:** 0/4
- **Implementer model:** claude-sonnet-4-6
- **Reviewer model:** codex-gpt-5.4
- **Worktree:** /Users/barneyhussey-yeo/GitHub/personal/.orchestrator-worktrees/hurlicane/wf-c6b64559 (branch: workflow/fix-standalone-print-job-completion-dete-c6b64559)

## Job History (last 10)
| ID | Phase | Status | Model | Title |
|----|-------|--------|-------|-------|
| 4a317283 | implement | done | claude-sonnet-4-6 | [Workflow C6] Implement |
| 00939e7b | review | done | codex-gpt-5.4 | [Workflow C7] Review |
| 30950b4c | implement | done | claude-sonnet-4-6 | [Workflow C7] Implement |
| 776bc304 | review | done | codex-gpt-5.4 | [Workflow C7] Review |
| 57614f97 | implement | done | claude-sonnet-4-6 | [Workflow C7] Implement |
| ad85a31a | implement | done | claude-sonnet-4-6 | [Workflow C7] Implement (resumed) |
| ef1c49ab | review | done | codex-gpt-5.4 | [Workflow C7] Review |
| 7561e509 | implement | done | claude-sonnet-4-6 | [Workflow C7] Implement |
| 6a2afe3c | review | done | codex-gpt-5.4 | [Workflow C8] Review |
| 3d9a1d92 | implement | done | claude-sonnet-4-6 | [Workflow C8] Implement |

## Failed Jobs (last 5 with details)
No failed jobs.

## Total Job Stats
- Total: 65
- Done: 65
- Failed: 0
- Cancelled: 0
- Success rate: 100%

## Git State
### Working tree status
```
M src/server/orchestrator/PtyManager.ts
?? src/test/pty-manager.test.ts
```

### Last 3 commits
```
85c446d fix: atomic recovery ledger operations to prevent race conditions
f069bde refactor: audit and fix silent catch blocks across orchestrator
8b1c7f1 fix: use --print mode for all non-interactive Claude jobs
```

## Latest Worklog Entry
```
(no worklog found)
```

## Plan (truncated)
```
# Plan

## Goal
Fix standalone non-interactive (`--print` mode) jobs being incorrectly marked failed or done without validating their final ndjson outcome by making every standalone completion path resolve from the final ndjson `result` event first, then commit history only when no `result` event exists.

## Review Findings

1. **Cycle 8 review: `attachPty()` still has two standalone attach paths that bypass the required `result -> commits -> fail` contract.** `src/server/orchestrator/PtyManager.ts:602-608` still marks any already-dead tmux session as `done` immediately, and `src/server/orchestrator/PtyManager.ts:702-706` still marks the exhausted-attach + tmux-already-gone path as `failed` directly from the PTY attach error. Both branches can misclassify a completed standalone print job whose final `result` event or commit evidence already exists.
2. **Cycle 8 review: `StuckJobWatchdog.statusFromLog()` still accepts forbidden turn events for standalone recovery.** `src/server/orchestrator/StuckJobWatchdog.ts:63-72` still returns terminal status from `turn.completed` and `turn.failed`, so dead-session standalone recovery can still succeed or fail without a final `result` event.
3. **Cycle 8 review: the `pty-manager` regression tests are still helper-level and still red.** `src/test/pty-manager.test.ts:138-239` still assert against `_statusFromNdjsonForTest()` instead of the real attach/exit completion paths, and `npm test` on April 5, 2026 still fails in those assertions because the helper returns `null` instead of the expected terminal status.
4. **Cycle 8 review: watchdog regression coverage is still placeholder-only.** `src/test/stuck-job-watchdog.test.ts:27-46` are still unimplemented stubs, so the dead-session standalone recovery path remains effectively untested.

## Assessment

### Current Gap
The branch is still not shippable for this workflow:
- `attachPty()` still has standalone attach-time shortcuts that skip the required `result -> commits -> fail` contract.
- `StuckJobWatchdog.statusFromLog()` still treats turn events as standalone terminal evidence.
- The `pty-manager` tests are still aimed at private helper seams instead of the production completion paths, and three of them are still red.
- The watchdog regression tests remain placeholder stubs.
- `npm test` still fails.

### Test Status
`npm test` failed on April 5, 2026 in `src/test/pty-manager.test.ts` with three failing assertions expecting `_statusFromNdjsonForTest()` to return `done` / `failed`, but receiving `null` instead.

## Milestones

- [ ] **M1: Apply the standalone `result -> commits -> fail` contract to every `attachPty()` terminal path** [M] — In addition to the fallback-poll and main `onExit()` branches, fix both remaining attach-time shortcuts in `attachPty()` so standalone non-interactive, non-debate, non-workflow jobs never resolve directly from tmux absence or PTY attach failure. This includes the early `!isTmuxSessionAlive(agentId)` return and the exhausted-at
```
