# Workflow Blocked Diagnostic

## Summary
- **Title:** Extract WorkflowFinalization from WorkflowManager.ts
- **ID:** fa33360e-b9c4-4861-af14-f90e73dce67a
- **Blocked at:** 2026-04-05T07:00:22.938Z
- **Reason:** Phase 'review' job e16c2170 failed (task_failure)
- **Phase:** review
- **Cycle:** 2/10
- **Milestones:** 0/3
- **Implementer model:** claude-sonnet-4-6
- **Reviewer model:** codex-gpt-5.4
- **Worktree:** /Users/barneyhussey-yeo/GitHub/personal/.orchestrator-worktrees/hurlicane/wf-fa33360e (branch: workflow/extract-workflowfinalization-from-workfl-fa33360e)

## Job History (last 10)
| ID | Phase | Status | Model | Title |
|----|-------|--------|-------|-------|
| 3c6a79f7 | assess | done | claude-sonnet-4-6 | [Workflow C0] Assess |
| 18aee544 | assess | done | claude-sonnet-4-6 | [Workflow C0] Assess quick repair |
| a96ad5e4 | assess | done | claude-sonnet-4-6 | [Workflow C0] Assess diagnostic repair |
| fbd5df59 | assess | done | claude-sonnet-4-6 | [Workflow C0] Assess full re-assess repair |
| 29db8301 | review | done | codex-gpt-5.4 | [Workflow C1] Review |
| 6fe2a810 | implement | done | claude-sonnet-4-6 | [Workflow C1] Implement |
| f8f6e360 | review | done | codex-gpt-5.4 | [Workflow C1] Review |
| 9b3e7334 | implement | done | claude-sonnet-4-6 | [Workflow C1] Implement |
| e16c2170 | review | failed | codex-gpt-5.4 | [Workflow C2] Review |

## Failed Jobs (last 5 with details)
### [Workflow C2] Review
- **Job ID:** e16c2170 | **Agent ID:** f13918cc
- **Phase:** review | **Model:** codex-gpt-5.4
- **Exit code:** n/a | **Turns used:** n/a | **Cost:** $n/a
- **DB Error:**
```
Agent process not found on restart.
```
- **Agent output (last lines):**
```
(no log output available)
```


## Total Job Stats
- Total: 9
- Done: 8
- Failed: 1
- Cancelled: 0
- Success rate: 89%

## Git State
### Working tree status
```
(clean)
```

### Last 3 commits
```
85c446d fix: atomic recovery ledger operations to prevent race conditions
f069bde refactor: audit and fix silent catch blocks across orchestrator
8b1c7f1 fix: use --print mode for all non-interactive Claude jobs
```

## Latest Worklog Entry
```
(no worklog found)
```

## Plan (truncated)
```
## Plan: Extract WorkflowFinalization from WorkflowManager

Goal: Land one focused extraction from `WorkflowManager.ts` by moving the finalization implementation into `WorkflowFinalization.ts` while preserving the existing public API from `WorkflowManager.ts` and avoiding new circular imports.

### Review improvements applied

1. The original milestone 1 understated the real dependency boundary. In the current code, `pushAndCreatePr` calls `ensureWorktreeBranch`, `countBranchCommits`, `_buildPrBody`, `queries.getNote`, and `updateAndEmit`; `finalizeWorkflow` also calls `getPrCreationOutcome` and `updateAndEmit`. The extraction plan must therefore either move the minimal helper closure with the finalization code or define explicit injected dependencies up front.
2. The plan did not explicitly protect the existing `WorkflowManager.js` runtime surface used by tests and mocks. Multiple tests import or `vi.mock()` named exports from `WorkflowManager.js`, so preserving named value exports and module-load behavior needs to be an acceptance criterion, not an implicit side effect of `npm test`.
3. `_removeWorktree` is part of the extraction scope but not part of the public API contract. The plan should state that it moves into `WorkflowFinalization.ts` as an internal helper and does not become a new public export.
4. The requested `~200 lines` target is only realistic if the extraction stays tightly scoped. If the minimal dependency closure makes the new module slightly larger, correctness and a clean boundary should win over forcing an arbitrary line count.

### Milestones

- [ ] **1. Create `src/server/orchestrator/WorkflowFinalization.ts` with the finalization implementation and an explicit dependency boundary**
- [ ] **2. Update `WorkflowManager.ts` to preserve the current import surface and avoid a module cycle**
- [ ] **3. Verify behavior, export contract, and tests**

---

### Milestone 1 detail: WorkflowFinalization.ts

Acceptance criteria:
- `WorkflowFinalization.ts` owns the implementation of `pushAndCreatePr`, `finalizeWorkflow`, `cleanupWorktree`, and `_removeWorktree`.
- `_removeWorktree` remains internal to the new module; do not add it to the public export surface from `WorkflowManager.ts`.
- Before moving code, choose and implement one clear boundary for the helpers those functions need:
  - move the minimal dependency closure into `WorkflowFinalization.ts`, or
  - keep those helpers in `WorkflowManager.ts` but pass them in via thin wrappers/dependencies so `WorkflowFinalization.ts` does not import `WorkflowManager.ts`.
- If helper colocation is cleaner, the minimal dependency closure may include `getPrCreationOutcome`, `_buildPrBody`, `countBranchCommits`, and their private git helpers, but it must not drag in unrelated blocked-diagnostic responsibilities.
- Do not move `BLOCKED_LOG_DIR` or `writeBlockedDiagnostic` in this cycle unless they are proven to be unavoidable for the selected dependency boundary.
- Keep the extraction bounded to
```
