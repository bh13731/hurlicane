# Workflow Blocked Diagnostic

## Summary
- **Title:** Autonomous Agent Run: ## API Documentation & Performance — Get to 10/10

- **ID:** ad50f5d5-4145-4f50-bf6f-9d64a544ca34
- **Blocked at:** 2026-04-08T20:00:46.129Z
- **Reason:** Startup worktree health check failed: Worktree recreation failed: Command failed: git worktree add "/Users/barneyhussey-yeo/GitHub/personal/.orchestrator-worktrees/ceo-dashboard/wf-ad50f5d5" "workflow/autonomous-agent-run-api-documentation-p-ad50f5d5"
fatal: invalid reference: workflow/autonomous-agent-run-api-documentation-p-ad50f5d5

- **Phase:** implement
- **Cycle:** 1/3
- **Milestones:** 0/4
- **Implementer model:** claude-sonnet-4-6
- **Reviewer model:** codex
- **Worktree:** /Users/barneyhussey-yeo/GitHub/personal/.orchestrator-worktrees/ceo-dashboard/wf-ad50f5d5 (branch: workflow/autonomous-agent-run-api-documentation-p-ad50f5d5)

## Job History (last 10)
| ID | Phase | Status | Model | Title |
|----|-------|--------|-------|-------|
| 5a3f0161 | assess | done | codex | [Workflow C0] Assess |
| 8047918c | review | done | codex | [Workflow C1] Review |
| bca99ef2 | implement | queued | claude-sonnet-4-6 | [Workflow C1] Implement |

## Failed Jobs (last 5 with details)
No failed jobs.

## Total Job Stats
- Total: 3
- Done: 2
- Failed: 0
- Cancelled: 0
- Success rate: 67%

## Git State
(git state unavailable)

## Latest Worklog Entry
```
(no worklog found)
```

## Plan (truncated)
```
# Plan

## Goal
Bring API and architecture documentation up to date while adding a small server-side cache for Mode-backed reads and documenting the database schema in place.

## Assessment
The codebase has no centralized API route reference yet, and the brief is already behind the current implementation: there are seven production/example API surfaces under `/api`, with sync authorization split between cron bearer auth and CEO-only Clerk auth. Dashboard access control is also enforced in two different ways: some pages redirect directly while the People subtree inherits its gate from `src/app/dashboard/people/layout.tsx`, which the current plan did not require the docs to call out. `src/lib/data/mode.ts` reads Postgres on every call with no memoization, and the same `(section, category)` pairs are requested repeatedly from parallel loaders (`chart-data.ts`, `metrics.ts`, `people.ts`) during adjacent or even concurrent server renders. A cache that only stores completed results would still allow duplicate queries while the first read is in flight, and caching degraded fallback values would incorrectly pin transient DB issues for the full TTL. `src/lib/db/schema.ts` still has sparse inline comments rather than per-table and per-column JSDoc, and `CLAUDE.md` still describes an older sync/runtime shape, narrower API surface, six-table schema, and a much smaller test footprint than the current repo.

## Milestones
- [ ] **M2: Add server-only Mode read cache** [M] — Add a module-local in-memory TTL cache around `getReportData()` in `src/lib/data/mode.ts` with a 60s default lifetime and cache keys scoped by `section` plus `category`. Acceptance criteria: the cache is server-only, concurrent same-key calls share one in-flight DB read instead of issuing duplicates, successful reads are reused within TTL, thrown errors and degraded fallback results are not cached, and Vitest coverage verifies cache hits, TTL expiry, key separation, and no error/fallback poisoning.
- [ ] **M1: Add route reference docs** [M] — Create `API_ROUTES.md` documenting every current API route (`/api/cron`, `/api/sync/mode`, `/api/sync/slack`, `/api/sync/management-accounts`, `/api/sync/cancel`, `/api/squads`, `/api/sentry-example-api`) and every dashboard route (`/dashboard`, section pages, people subroutes, admin pages). Acceptance criteria: each entry includes path, method(s), auth mode or minimum role, request/query parameters (including `force=1` where supported), success and common error response shapes/status codes, data sources, and real curl examples for cron and manual sync triggers; dashboard docs explicitly note whether access is enforced in the page itself or inherited from a parent layout.
- [ ] **M4: Refresh CLAUDE.md to current architecture** [M] — Update `CLAUDE.md` so it matches the live codebase after M2/M1. Acceptance criteria: it covers the sync coordinator/runtime/request-auth/response/error/phase-tracker/worker-state modules, config modules under `src/lib/config
```
