# Workflow Blocked Diagnostic

## Summary
- **Title:** Sentry fix [investor-pipeline]: HttpError: <HttpError 429 when requesting https://gmail.googleapis.com/gmail/v1/
- **ID:** 038e520c-05be-471c-9c89-bf2c5fe7bf6b
- **Blocked at:** 2026-04-08T22:06:06.778Z
- **Reason:** Workflow stuck after failed review job c638fcc0
- **Phase:** review
- **Cycle:** 2/5
- **Milestones:** 0/4
- **Implementer model:** claude-opus-4-6
- **Reviewer model:** codex
- **Worktree:** /Users/barneyhussey-yeo/GitHub/personal/.orchestrator-worktrees/investor-pipeline/wf-038e520c (branch: workflow/sentry-fix-investor-pipeline-httperror-h-038e520c)

## Job History (last 10)
| ID | Phase | Status | Model | Title |
|----|-------|--------|-------|-------|
| 3f8f96db | assess | done | claude-opus-4-6 | [Workflow C0] Assess |
| 2e70a49b | assess | failed | claude-opus-4-6 | [Workflow C0] Assess quick repair |
| 5b1e1676 | assess | done | codex | [Workflow C0] Assess (fallback) |
| b548db93 | review | done | codex | [Workflow C1] Review |
| 3819cb6f | implement | done | codex | [Workflow C1] Implement |
| 5973f861 | review | failed | codex | [Workflow C2] Review |
| c638fcc0 | review | failed | claude-opus-4-6 | [Workflow C2] Review (fallback) |

## Failed Jobs (last 5 with details)
### [Workflow C0] Assess quick repair
- **Job ID:** 2e70a49b | **Agent ID:** e1b56a02
- **Phase:** assess | **Model:** claude-opus-4-6
- **Exit code:** n/a | **Turns used:** n/a | **Cost:** $n/a
- **DB Error:**
```
API Error: 429 {"type":"error","error":{"type":"rate_limit_error","message":"This request would exceed your organization's rate limit of 30,000 input tokens per minute (org: 62dc307d-268c-4e85-b5a6-86a2613aab61, model: claude-opus-4-6). For details, refer to: https://docs.claude.com/en/api/rate-limits. You can see the response headers for current usage. Please reduce the prompt length or the maximum tokens requested, or try again later. You may also contact sales at https://claude.com/contact-sales to discuss your options for a rate limit increase."},"request_id":"req_011CZs3ur3SoR97vzsoPEVXG"}
```
- **Agent output (last lines):**
```
[ERROR] rate_limit
[ERROR] unknown
[RESULT] "API Error: 429 {\"type\":\"error\",\"error\":{\"type\":\"rate_limit_error\",\"message\":\"This request would exceed your organization's rate limit of 30,000 input tokens per minute (org: 62dc307d-268c-4e85-b5a6-86a2613aab61, model: claude-opus-4-6). For details, refer to: https://docs.claude.com/en
```

### [Workflow C2] Review
- **Job ID:** 5973f861 | **Agent ID:** 3a7f7e63
- **Phase:** review | **Model:** codex
- **Exit code:** -1 | **Turns used:** n/a | **Cost:** $n/a
- **DB Error:**
```
Reading prompt from stdin...
```
- **Agent output (last lines):**
```
(no log output available)
```

### [Workflow C2] Review (fallback)
- **Job ID:** c638fcc0 | **Agent ID:** fb336e77
- **Phase:** review | **Model:** claude-opus-4-6
- **Exit code:** n/a | **Turns used:** n/a | **Cost:** $n/a
- **DB Error:**
```
PTY attach failed after 4 attempts: posix_spawnp failed.
```
- **Agent output (last lines):**
```
[ERROR] rate_limit
```


## Total Job Stats
- Total: 7
- Done: 4
- Failed: 3
- Cancelled: 0
- Success rate: 57%

## Git State
### Working tree status
```
(clean)
```

### Last 3 commits
```
0a4b747 fix: add deploy lock, stop-before-mutate, and compile gate to watch-deploy
e5f6927 fix: stop dashboard before git mutations in poll-deploy.sh
d052993 fix: skip startup when deploy lock is held
```

## Latest Worklog Entry
```
## Cycle 1 — M1: Fix Gmail Retry Capture Semantics
**Owner:** Implementer
**Timestamp:** 2026-04-08T22:03:21Z

### What changed
- `lib/gmail.py`: changed `with_backoff()` so retryable Gmail 429/403 quota errors are retried without intermediate Sentry capture; terminal retry exhaustion now logs once and captures once before re-raising.
- `tests/test_gmail_utils.py`: added focused assertions for no Sentry capture on successful retries, single capture on terminal rate-limit exhaustion, and immediate single capture for non-rate-limit failures.

### Commits
- None; the required commit is blocked by a branch mismatch in the source-of-truth checkout.

### Test results
- `tests/test_gmail_utils.py`: 23 passed, 0 failed
- `tests/test_gmail_utils.py tests/test_diligence.py`: 82 passed, 0 failed

### Blockers
- The required source checkout `/Users/barneyhussey-yeo/GitHub/personal/investor-pipeline` is on branch `workflow/sentry-fix-syntaxerror-invalid-syntax-co-4ca4aa58`, while the run requires commits on `workflow/sentry-fix-investor-pipeline-httperror-h-038e520c`. That required branch exists but is already checked out in another linked worktree, and the task guardrails prohibit switching branches or moving implementation to another checkout.

### Next step
Resolve the source-checkout branch/worktree mismatch, then commit the tested M1 changes on `workflow/sentry-fix-investor-pipeline-httperror-h-038e520c` and mark the milestone complete.
```

## Plan (truncated)
```
# Plan

## Goal
Reduce Gmail 429 Sentry noise and harden diligence refreshes so transient per-user Gmail concurrency limits do not create handled errors or destabilize DD extraction.

## Assessment
The failing stack originates in `dashboard_services/diligence.fetch_dd_email_bodies()` calling `lib.gmail.with_backoff()` around `messages.list()`. The current retry helper captures every exception to Sentry before deciding whether it is retryable, so transient 429s are reported on intermediate attempts. Separately, `fetch_dd_email_bodies()` catches the final exception and calls `sentry_sdk.capture_exception()` again before returning `[]`, which means terminal Gmail failures can be double-captured even though diligence already degrades gracefully. The underlying pressure is also real: `generate_diligence_data()` fans out DD firms with `ThreadPoolExecutor(max_workers=8)` while Gmail concurrency is capped globally at `GMAIL_CONCURRENCY = 5`, and diligence lacks the cross-trigger generation lock that deep dives already use across pipeline, auto-refresh, and the manual API route.

## Review Improvements
1. The original M1 covered retry semantics in `lib/gmail.py` but did not explicitly address the duplicate capture in `dashboard_services/diligence.fetch_dd_email_bodies()`. That gap matters because fixing `with_backoff()` alone would still leave handled diligence failures being captured again upstream.
2. The original M2 bundled two different failure modes into one vague milestone: per-run Gmail fanout (`ThreadPoolExecutor(max_workers=8)`) and overlapping diligence generations from pipeline/auto-refresh/manual triggers. The updated plan separates call-site degradation from concurrency control so each milestone is testable and fits in one implementation cycle.

## Milestones
- [ ] **M1: Fix Gmail Retry Capture Semantics** [S] — Update `lib/gmail.py` so retryable Gmail 429/403 quota errors are retried without calling Sentry on intermediate attempts. Acceptance: retryable attempts only emit structured logs with attempt counts; non-rate-limit failures are still surfaced immediately; exhausted rate-limit failures are surfaced at most once by the retry helper with predictable logging/capture behavior.
- [ ] **M2: De-duplicate Diligence Gmail Failure Handling** [S] — Narrow the diligence Gmail fallback path in `dashboard_services/diligence.py` so expected Gmail throttling does not get captured again after `with_backoff()` exhausts. Acceptance: `fetch_dd_email_bodies()` still returns `[]` on Gmail failure so DD generation continues, rate-limit exhaustion is logged with firm/domain context without an extra Sentry capture, and unexpected non-rate-limit failures remain visible exactly once.
- [ ] **M3: Serialize and Right-Size Diligence Gmail Work** [M] — Harden diligence generation against real Gmail per-user concurrency limits. Acceptance: diligence generation has one explicit cross-trigger policy (prefer mirroring deep dives with a non-blocking generation lock that
```
