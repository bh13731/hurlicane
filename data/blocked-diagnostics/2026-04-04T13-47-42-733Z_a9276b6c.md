# Workflow Blocked Diagnostic

## Summary
- **Title:** Add state machine validation for workflow transitions
- **ID:** a9276b6c-2169-4d53-8478-baa62bae2614
- **Blocked at:** 2026-04-04T13:47:42.770Z
- **Reason:** 2 consecutive implement cycles with no milestone progress (7/9 complete)
- **Phase:** implement
- **Cycle:** 2/10
- **Milestones:** 7/9
- **Implementer model:** claude-sonnet-4-6
- **Reviewer model:** codex-gpt-5.4
- **Worktree:** /Users/barneyhussey-yeo/GitHub/personal/.orchestrator-worktrees/hurlicane/wf-a9276b6c (branch: workflow/add-state-machine-validation-for-workflo-a9276b6c)

## Job History (last 10)
| ID | Phase | Status | Model | Title |
|----|-------|--------|-------|-------|
| 3c6ede3c | assess | done | claude-sonnet-4-6 | [Workflow C0] Assess diagnostic repair |
| be2bf880 | assess | done | claude-sonnet-4-6 | [Workflow C0] Assess full re-assess repair |
| 409b0d55 | review | done | codex-gpt-5.4 | [Workflow C1] Review |
| bbff3f9a | implement | done | claude-sonnet-4-6 | [Workflow C1] Implement |
| bd228210 | review | done | codex-gpt-5.4 | [Workflow C1] Review |
| 7a58dec4 | implement | done | claude-sonnet-4-6 | [Workflow C1] Implement |
| 7c9b2bac | review | done | codex-gpt-5.4 | [Workflow C2] Review |
| 43820a95 | implement | done | claude-sonnet-4-6 | [Workflow C2] Implement |
| 1d166428 | review | done | codex-gpt-5.4 | [Workflow C2] Review |
| b79553cc | implement | done | claude-sonnet-4-6 | [Workflow C2] Implement |

## Failed Jobs (last 5 with details)
No failed jobs.

## Total Job Stats
- Total: 12
- Done: 12
- Failed: 0
- Cancelled: 0
- Success rate: 100%

## Git State
### Working tree status
```
(clean)
```

### Last 3 commits
```
8b1c7f1 fix: use --print mode for all non-interactive Claude jobs
7bad98b fix: auto-fallback from Codex to Claude for MCP-heavy workflow phases (#23)
06dc772 feat: unified Tasks dashboard — replace dual grids with single TaskFeed
```

## Latest Worklog Entry
```
(no worklog found)
```

## Plan (truncated)
```
## Plan: Add state machine validation for workflow transitions

### Goal
Add `WORKFLOW_TRANSITIONS` and a workflow-focused validator in `StateTransitions.ts`. Update `updateAndEmit` in `WorkflowManager.ts` to warn on illegal workflow status transitions without blocking the DB write or socket emit. Run `npm test`.

### Milestones

- [x] **M1**: Create `src/server/orchestrator/StateTransitions.ts` with `JOB_TRANSITIONS`, `WORKFLOW_TRANSITIONS`, and `DEBATE_TRANSITIONS` maps, plus the `validateTransition(entity, from, to, entityId?)` function that warn-logs illegal transitions without throwing.
- [x] **M2**: Import and call `validateTransition('workflow', previousStatus, fields.status, id)` inside `updateAndEmit` in `WorkflowManager.ts` before writing to the DB.
- [x] **M3**: Also call `validateTransition('job', ...)` in `queries.updateJobStatus` and `validateTransition('debate', ...)` in `queries.updateDebate` for full coverage.
- [x] **M4**: Write `src/test/state-transitions.test.ts` covering legal transitions, illegal transitions, unknown-from-status, same-status no-ops, undefined from-status, and entityId truncation.
- [x] **M5**: Verify `npm test` passes.
- [x] **M6**: Narrow the implementation to the requested workflow change: extend the existing `src/server/orchestrator/StateTransitions.ts` file rather than treating this as a new module, and avoid additional job/debate integration work unless it is already present and can remain unchanged without consuming review or implementation scope.
- [x] **M7**: Export a workflow-specific API from `src/server/orchestrator/StateTransitions.ts`, e.g. `validateWorkflowTransition(from, to, workflowId?)`, backed directly by `WORKFLOW_TRANSITIONS`; `WorkflowManager.updateAndEmit` should call this workflow-specific entrypoint so the implementation matches the task language even if a generic helper remains as an internal detail.
- [ ] **M8**: Add a focused `WorkflowManager` integration test that exercises `updateAndEmit` through a real workflow state change and proves both warn-only paths at that seam: a legal workflow status transition with no `console.warn`, and an illegal workflow status transition where `console.warn` fires exactly for the transition check while `queries.updateWorkflow(...)` still persists the new status and `socket.emitWorkflowUpdate(...)` still receives the updated workflow object.
- [ ] **M9**: Re-run `npm test` after the new `WorkflowManager` coverage lands and confirm the suite still passes, including the existing `state-transitions` helper tests and the new `updateAndEmit` warning-path coverage.

### Status
The implementation change is correct and currently passes the full suite, but the task’s core warn-only contract is still only proven at the helper level. The remaining work is a single integration test at `WorkflowManager.updateAndEmit(...)`, followed by one more `npm test` run.

### Review Notes
1. The code change itself is correct and scoped. `src/server/orchestrator/StateTransi
```
