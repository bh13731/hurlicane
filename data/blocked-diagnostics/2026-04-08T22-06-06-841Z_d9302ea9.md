# Workflow Blocked Diagnostic

## Summary
- **Title:** Sentry fix [hurlicane]: WorkflowBlocked: Workflow blocked: Autonomous Agent Run: ## Error Boundaries & R
- **ID:** d9302ea9-fd29-4cea-b921-64c8602eda78
- **Blocked at:** 2026-04-08T22:06:06.873Z
- **Reason:** Workflow stuck after failed implement job 60833703
- **Phase:** implement
- **Cycle:** 2/5
- **Milestones:** 1/3
- **Implementer model:** claude-opus-4-6
- **Reviewer model:** codex
- **Worktree:** /Users/barneyhussey-yeo/GitHub/personal/.orchestrator-worktrees/hurlicane/wf-d9302ea9 (branch: workflow/sentry-fix-hurlicane-workflowblocked-wor-d9302ea9)

## Job History (last 10)
| ID | Phase | Status | Model | Title |
|----|-------|--------|-------|-------|
| 573d685b | assess | failed | claude-opus-4-6 | [Workflow C0] Assess |
| d4bac39f | assess | done | codex | [Workflow C0] Assess (fallback) |
| 6a5bc4db | review | done | codex | [Workflow C1] Review |
| 2969c4ae | implement | failed | codex | [Workflow C1] Implement |
| 480d3f64 | implement | done | claude-opus-4-6 | [Workflow C1] Implement (fallback) |
| 21929821 | review | done | codex | [Workflow C2] Review |
| 8553bbb8 | implement | failed | codex | [Workflow C2] Implement |
| 60833703 | implement | failed | claude-opus-4-6 | [Workflow C2] Implement (fallback) |

## Failed Jobs (last 5 with details)
### [Workflow C0] Assess
- **Job ID:** 573d685b | **Agent ID:** 6c39619a
- **Phase:** assess | **Model:** claude-opus-4-6
- **Exit code:** n/a | **Turns used:** n/a | **Cost:** $n/a
- **DB Error:**
```
Agent session ended without a final result event or new commits.
```
- **Agent output (last lines):**
```
[ERROR] rate_limit
Now let me look at the resolution logic and the infrastructure failure detection more closely.
[ERROR] rate_limit
Now let me look at the infrastructure failure detection in WorkflowManager more carefully, specifically the `hasNoTurns && hasNoCost && hasNoLogOutput` check.
```

### [Workflow C1] Implement
- **Job ID:** 2969c4ae | **Agent ID:** 5b9a57ee
- **Phase:** implement | **Model:** codex
- **Exit code:** -1 | **Turns used:** n/a | **Cost:** $n/a
- **DB Error:**
```
Reading prompt from stdin...
```
- **Agent output (last lines):**
```
(no log output available)
```

### [Workflow C2] Implement
- **Job ID:** 8553bbb8 | **Agent ID:** 2c3519a4
- **Phase:** implement | **Model:** codex
- **Exit code:** -1 | **Turns used:** n/a | **Cost:** $n/a
- **DB Error:**
```
Reading prompt from stdin...
```
- **Agent output (last lines):**
```
(no log output available)
```

### [Workflow C2] Implement (fallback)
- **Job ID:** 60833703 | **Agent ID:** 8cded73f
- **Phase:** implement | **Model:** claude-opus-4-6
- **Exit code:** n/a | **Turns used:** n/a | **Cost:** $n/a
- **DB Error:**
```
PTY attach failed after 4 attempts: posix_spawnp failed.
```
- **Agent output (last lines):**
```
[ERROR] rate_limit
```


## Total Job Stats
- Total: 8
- Done: 4
- Failed: 4
- Cancelled: 0
- Success rate: 50%

## Git State
### Working tree status
```
(clean)
```

### Last 3 commits
```
c210587 fix: preserve PTY attach errors through standalone print job finalization
023a923 fix: fail fast when job work_dir does not exist (#40)
65e1aea test: cover PTY spawning-state cleanup
```

## Latest Worklog Entry
```
## Cycle 1 — M1: Preserve infrastructure failure signals for auto-exit jobs
**Owner:** Implementer
**Timestamp:** 2026-04-08T22:59:00Z

### What changed
- `src/server/orchestrator/PtyManager.ts`: Added `_ptyAttachErrors` map to store PTY attach errors per agent when retries exhaust. In `attachPty`, the real error (e.g. `posix_spawnp failed`) is now stored in the map before falling through to the poll/finalize path. In `resolveStandalonePrintJobOutcome`, the `no_terminal_evidence` fallback now checks this map first — if a prior PTY error exists, it includes that error in the `errorMessage` field (e.g. `PTY attach failed: posix_spawnp failed.`) so `FailureClassifier` can correctly classify it as `launch_environment` instead of `task_failure`.
- Added `_setPtyAttachErrorForTest` helper for test injection and updated `_resetPtyManagerStateForTest` to clear the new map.
- Added cleanup of `_ptyAttachErrors` in early-return paths of `finalizeStandalonePrintJob` (already-terminal agents, cancelled agents).

### Commits
- `c210587` fix: preserve PTY attach errors through standalone print job finalization

### Test results
- All test suites: 862 passed, 0 failed (65 test files)

### Blockers
- None

### Next step
M2: Harden workflow pre-start failure recovery — tighten the "failed before start" detection to cover bootstrap-only NDJSON cases.
```

## Plan (truncated)
```
# Plan

## Goal
Prevent auto-exit workflow jobs from being misclassified as generic task failures when PTY attach or other startup infrastructure fails, so the workflow recovers instead of blocking and emitting a `WorkflowBlocked` Sentry error.

## Review Outcome
Cycle 1's `PtyManager` change is acceptable for M1. The preserved-error path now keeps a prior classified infrastructure failure on the agent record instead of always collapsing to the generic `no_terminal_evidence` message, which is the core bug shown in Sentry. No additional correctness bug was found in this cycle, so no fix milestone was added.

## Validation Snapshot
- Reviewed the uncommitted diff in `src/server/orchestrator/PtyManager.ts` from the target repo checkout.
- Ran `npm test -- src/test/pty-manager.test.ts src/test/error-handling-recovery.test.ts` from `/Users/barneyhussey-yeo/GitHub/personal/hurlicane`.
- Ran `npx tsc -p tsconfig.server.json --noEmit` from `/Users/barneyhussey-yeo/GitHub/personal/hurlicane`.
- Existing tests passed, but there is still no direct regression test covering the new preserved-error branch; that remains M3 work rather than a blocker for M1.

## Milestones
- [x] **M1: Preserve infrastructure failure signals for auto-exit jobs** [M] — Update the PTY/standalone-finalization path so a failed PTY attach or equivalent startup infrastructure error is retained in classifier-visible state instead of being collapsed to the generic "Agent session ended without a final result event or new commits." message. Acceptance: when PTY attach retries exhaust after a captured launch/resource error, the persisted agent failure text still contains that original error and `classifyJobFailure` resolves it as a fallback-eligible infrastructure kind such as `launch_environment`.
- [ ] **M2: Harden workflow pre-start failure recovery** [M] — Tighten `WorkflowManager`'s "failed before start" detection so jobs with zero turns, zero cost, zero commits, and only bootstrap/startup NDJSON events are treated as infrastructure failures and retried in-place rather than blocking the workflow. Define the bootstrap-only predicate explicitly and keep it narrow: it should allow startup events such as `hook_started`, `hook_response`, `init`, and similarly pre-turn bootstrap events only. Acceptance: the observed startup-only NDJSON case respawns the same phase/cycle without blocking, while any job with real work signals (`assistant`/`tool`/`result` output, persisted turns or cost, or commits) does not take this infrastructure-recovery path.
- [ ] **M3: Add regression coverage for PTY fallback and workflow recovery** [M] — Extend the existing targeted suites to lock the boundary on both sides: `src/test/pty-manager.test.ts` for preserved PTY attach errors, and the relevant workflow/recovery tests for startup-only retry vs real task-failure blocking. Acceptance: tests prove that PTY attach exhaustion preserves the original launch error, startup-only NDJSON is retried as pre-start failure, 
```
