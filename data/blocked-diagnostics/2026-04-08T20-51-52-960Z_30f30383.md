# Workflow Blocked Diagnostic

## Summary
- **Title:** Sentry fix [investor-pipeline]: Gmail API rate limiting
- **ID:** 30f30383-8d58-47e6-bfc3-d9ce6f5d8c4a
- **Blocked at:** 2026-04-08T20:51:53.001Z
- **Reason:** Reached max cycles (5) with 3/4 milestones complete
- **Phase:** idle
- **Cycle:** 5/5
- **Milestones:** 3/4
- **Implementer model:** claude-opus-4-6
- **Reviewer model:** codex
- **Worktree:** /Users/barneyhussey-yeo/GitHub/personal/.orchestrator-worktrees/investor-pipeline/wf-30f30383 (branch: workflow/sentry-fix-investor-pipeline-gmail-api-r-30f30383)

## Job History (last 10)
| ID | Phase | Status | Model | Title |
|----|-------|--------|-------|-------|
| 1b3b0c10 | implement | done | claude-opus-4-6 | [Workflow C3] Implement |
| 2dbe0beb | review | done | codex | [Workflow C3] Review |
| ffc1076d | implement | failed | claude-opus-4-6 | [Workflow C3] Implement |
| d2029176 | implement | done | codex | [Workflow C3] Implement (fallback) |
| 006448be | review | done | codex | [Workflow C4] Review |
| 821cdba7 | implement | done | claude-opus-4-6 | [Workflow C4] Implement |
| 614238a7 | review | done | codex | [Workflow C4] Review |
| 5f5edae7 | implement | done | claude-opus-4-6 | [Workflow C4] Implement |
| dd5dcca5 | review | done | codex | [Workflow C5] Review |
| 080f7a44 | implement | done | claude-opus-4-6 | [Workflow C5] Implement |

## Failed Jobs (last 5 with details)
### [Workflow C0] Assess
- **Job ID:** 570c41bc | **Agent ID:** 646fe224
- **Phase:** assess | **Model:** claude-opus-4-6
- **Exit code:** n/a | **Turns used:** n/a | **Cost:** $n/a
- **DB Error:**
```
Agent session ended without a final result event or new commits.
```
- **Agent output (last lines):**
```
Now let me see how `_analyze_firm` is called — the outer loop that processes multiple firms:
```

### [Workflow C3] Implement
- **Job ID:** ffc1076d | **Agent ID:** a6793cdd
- **Phase:** implement | **Model:** claude-opus-4-6
- **Exit code:** n/a | **Turns used:** n/a | **Cost:** $n/a
- **DB Error:**
```
Agent session ended without a final result event or new commits.
```
- **Agent output (last lines):**
```
[ERROR] rate_limit
Good, on the correct branch. Let me read the current state of the files I need to modify.
Now let me read the relevant parts of `analysis.py` — specifically `_analyze_firm` and the Gmail gate handling.
```


## Total Job Stats
- Total: 17
- Done: 15
- Failed: 2
- Cancelled: 0
- Success rate: 88%

## Git State
### Working tree status
```
(clean)
```

### Last 3 commits
```
daddc9d fix: release gmail analysis gate on exceptions
681140e fix: remove per-firm Gmail fan-out and release gate before LLM work
925aafb fix: gate Gmail concurrency at refresh boundary to prevent 429 errors
```

## Latest Worklog Entry
```
## Cycle 3 — M3: Make refresh-level Gmail gate release exception-safe
**Owner:** Implementer
**Timestamp:** 2026-04-08T20:44:38Z

### What changed
- `dashboard_services/analysis.py`: Added a one-shot Gmail gate releaser and wrapped `_analyze_firm` in `try`/`finally` so the refresh semaphore is released exactly once on cache-hit, success, and exception paths while still dropping immediately after Gmail I/O on the happy path.
- `dashboard_services/rejections.py`: Applied the same one-shot gate release pattern to `_analyze_nip_firm`, covering cache hits, Gmail fetch failures, and post-Gmail decline-analysis exceptions.

### Commits
- `daddc9d` fix: release gmail analysis gate on exceptions

### Test results
- `tests/test_analysis.py tests/test_rejections_service.py tests/test_flask_endpoints.py`: 229 passed, 0 failed
- `tests/`: 1180 passed, 23 skipped, 0 failed

### Blockers
- None

### Next step
M4: add focused regression tests for outer refresh gating, exception-safe semaphore release, and the sequential Gmail phase.
```

## Plan (truncated)
```
# Plan

## Goal
Eliminate Gmail 429 "Too many concurrent requests for user" failures in the analysis pipeline without changing per-firm outputs or cache semantics.

## Assessment
Cycle 1 added explicit refresh-level semaphores in `dashboard_services/refresh.py` and kept analyzed/skipped progress accounting intact, so the refresh path no longer lets all `OPENAI_WORKERS` firms enter analysis at once. Cycle 2 removed the inner per-firm `ThreadPoolExecutor` fan-out in `_analyze_firm` so Gmail reads are sequential per firm, and changed the gate protocol so the refresh layer acquires the semaphore before dispatching, passes it into `_analyze_firm`/`_analyze_nip_firm`, and each function releases it after Gmail I/O completes — LLM work then runs without holding a Gmail slot. Cycle 3 hardened that handoff by wrapping both analyzers in one-shot gate releasers guarded by `try`/`finally`, so cache hits, Gmail successes, `get_thread_local_service` failures, Gmail fetch errors, and downstream LLM exceptions all return the refresh permit exactly once without shrinking concurrency for the rest of the run. Cycle 5 review on branch `workflow/sentry-fix-investor-pipeline-gmail-api-r-30f30383` found no new correctness bugs in the current implementation: `git diff HEAD~1` is scoped to the exception-safe releasers in `dashboard_services/analysis.py` and `dashboard_services/rejections.py`, the working tree is clean, and both the targeted suites (`229 passed`) and neutral full suite (`1180 passed, 23 skipped`) are green. The remaining gap is still explicit regression coverage: there are still no direct tests for refresh-level semaphore handoff, one-shot release on analyzer exceptions, or the absence of inner Gmail fan-out / refresh-wrapper permit leaks.

## Milestones
- [x] **M1: Separate or limit Gmail work at the refresh boundary** [M] — Change the refresh orchestration so the analysis Gmail phase is not driven directly by the 8-worker OpenAI pool. Either introduce a smaller Gmail-specific worker budget or otherwise guarantee that the refresh cannot start more than the intended number of Gmail-bound firm analyses at once. Acceptance criteria: `dashboard_services/refresh.py` no longer allows `OPENAI_WORKERS` concurrent firms to enter Gmail fetches simultaneously; the limit is explicit in code, and the existing analyzed/skipped progress accounting is preserved.
- [x] **M2: Remove inner per-firm Gmail fan-out and release the refresh gate before LLM work** [M] — Refactor `_analyze_firm` so the Gmail reads happen in a deterministic phase under the refresh gate, then run summary/sentiment/momentum/reply work after Gmail data is loaded without holding that gate. Apply the same phase boundary to rejected/NIP analysis so Gmail throttling does not serialize decline-analysis LLM work. Acceptance criteria: `_analyze_firm` no longer submits `fetch_last_thread` / `fetch_email_history` to an inner `ThreadPoolExecutor`; no more than `GMAIL_ANALYSIS_CONCURRENCY` firms can be in Gmail 
```
