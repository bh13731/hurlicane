# Workflow Blocked Diagnostic

## Summary
- **Title:** Complete Task UX: API endpoint + UI (phase 2)
- **ID:** 22ed2f86-03be-44cf-bed0-91d41f3e2193
- **Blocked at:** 2026-04-04T07:34:22.215Z
- **Reason:** Phase 'implement' job a40f7812 failed (task_failure)
- **Phase:** implement
- **Cycle:** 3/25
- **Milestones:** 3/5
- **Implementer model:** claude-opus-4-6[1m]
- **Reviewer model:** codex-gpt-5.4
- **Worktree:** /Users/barneyhussey-yeo/GitHub/personal/.orchestrator-worktrees/wf-22ed2f86 (branch: workflow/complete-task-ux-api-endpoint-ui-phase-2-22ed2f86)

## Job History (last 10)
| ID | Phase | Status | Model | Title |
|----|-------|--------|-------|-------|
| 40f0560d | assess | done | claude-opus-4-6[1m] | [Workflow C0] Assess |
| c43ab2b3 | review | done | codex-gpt-5.4 | [Workflow C1] Review |
| 6fc0381d | implement | done | claude-opus-4-6[1m] | [Workflow C1] Implement |
| 606fbfad | review | done | codex-gpt-5.4 | [Workflow C2] Review |
| f1555d8c | implement | done | claude-opus-4-6[1m] | [Workflow C2] Implement |
| fcbe746f | review | done | codex-gpt-5.4 | [Workflow C3] Review |
| a40f7812 | implement | failed | claude-opus-4-6[1m] | [Workflow C3] Implement |

## Failed Jobs (last 5 with details)
### [Workflow C3] Implement
- **Job ID:** a40f7812 | **Agent ID:** 382c4537
- **Phase:** implement | **Model:** claude-opus-4-6[1m]
- **Exit code:** n/a | **Turns used:** n/a | **Cost:** $n/a
- **DB Error:**
```
Command failed: tmux new-session -d -s orchestrator-382c4537-78dc-4d5c-aaa3-406e1ad2068f -x 100 -y 50 /Users/barneyhussey-yeo/GitHub/personal/hurlicane/data/agent-scripts/382c4537-78dc-4d5c-aaa3-406e1ad2068f.sh
create window failed: fork failed: Device not configured

```
- **Agent output (last lines):**
```
(no log output available)
```


## Total Job Stats
- Total: 7
- Done: 6
- Failed: 1
- Cancelled: 0
- Success rate: 86%

## Git State
### Working tree status
```
(clean)
```

### Last 3 commits
```
1223f2e feat: add MCP create_task unified tool (M3)
5839f66 feat: add POST /api/tasks unified endpoint (M1)
17d0a51 feat: unified Task contract and normalization layer
```

## Latest Worklog Entry
```
## Cycle 2 — M2 (branch drift verification) + M3 (MCP create_task tool)
**Owner:** Implementer
**Timestamp:** 2026-04-04T08:32Z

### What changed
- **M2 verified as no-op**: Commit `5839f66` is already on branch `workflow/complete-task-ux-api-endpoint-ui-phase-2-22ed2f86`. `src/server/api/tasks.ts` exists and is mounted. The reviewer's branch drift concern was based on a different branch name (`workflow/unify-jobs-workflows-into-a-single-task--8463eece`) that doesn't match the active workflow branch. No code changes needed.

- `src/server/mcp/tools/createTask.ts`: New file — MCP `create_task` tool with Zod schema covering all `CreateTaskRequest` fields. Handler maps MCP input to `CreateTaskRequest`, validates via `validateTaskRequest()`, routes via `resolveTaskConfig()`. Job path uses `taskToJobRequest()` and inherits work_dir/model/project_id from calling agent's parent job. Workflow path uses `taskToWorkflowRequest()` and delegates to `createAutonomousAgentRun()`.

- `src/server/mcp/McpServer.ts`: Imported `createTaskHandler`/`createTaskSchema`, registered `create_task` tool with full schema. Added deprecation notes to `create_job` and `create_autonomous_agent_run` descriptions.

### Commits
- `1223f2e` feat: add MCP create_task unified tool (M3)

### Test results
- Full suite: 58 test files, 793 passed, 0 failed

### Blockers
- None

### Next step
M4: Unified task creation UI (TaskForm + Header/App wiring)
```

## Plan (truncated)
```
# Plan

## Goal
Build the unified Task creation flow: API endpoint, UI form, Header/App wiring, and MCP tool — all using the already-merged normalization layer.

## Assessment
The normalization layer (`src/shared/taskNormalization.ts`) is present with the shared task types in `src/shared/types.ts`, and the M1 endpoint commit (`5839f66`) plus the M3 MCP tool commit (`1223f2e`) exist on `workflow/complete-task-ux-api-endpoint-ui-phase-2-22ed2f86`.

Code review findings from cycle 3:
- `npm test` is green in the current checkout (`58` files, `793` tests passed), but the reviewed branch in the prompt is not the same as the currently checked-out branch, so the cycle review was performed directly against commits `5839f66` and `1223f2e` rather than the live worktree.
- **Issue 1: `create_task` does not actually accept the shared `CreateTaskRequest` contract.** In `src/server/mcp/tools/createTask.ts:17-48` and its registration in `src/server/mcp/McpServer.ts:390-417`, the tool exposes only a subset of fields and also renames contract fields to snake_case (`work_dir`, `depends_on`) instead of accepting the canonical task request shape (`workDir`, `dependsOn`). This breaks the stated requirement that MCP `create_task` accept the same fields as `CreateTaskRequest`.
- **Issue 2: the MCP job path is not behaviorally aligned with the unified task/job creation path.** `src/server/mcp/tools/createTask.ts:118-159` inserts a simplified job directly, so job-routed MCP tasks silently drop supported options such as `interactive`, `scheduledAt`, debate/pre-debate setup, smart-title generation parity, and any future logic centralized in `/api/tasks` or `/api/jobs`. The task contract is supposed to route through the same normalization and creation logic, not a reduced fork.
- **Issue 3: there is still no integration coverage for the new API or MCP surface.** `rg` over `src/test` found no `POST /api/tasks` or `create_task` tests, so the acceptance criteria for quick/reviewed/autonomous routing, validation failures, and legacy-endpoint regression coverage are still unmet.

## Milestones
- [x] **M1: `POST /api/tasks` endpoint** [M] — Create `src/server/api/tasks.ts` and mount it in `src/server/api/router.ts`. Accept `CreateTaskRequest`, validate with `validateTaskRequest()`, resolve with `resolveTaskConfig()`, then route to job creation or workflow creation. For job-routed tasks, reuse the existing `/api/jobs` creation behavior rather than a simplified insert path, including title fallback/smart-title follow-up, debate handling, socket emission, and queue nudge. For workflow-routed tasks, delegate through `createAutonomousAgentRun()` and emit the same workflow event shape as `/api/workflows`. Return a `CreateTaskResponse` with `task_type` plus the created entity payload. **Acceptance**: quick and reviewed payloads return `task_type: 'job'` with a created job; autonomous payloads return `task_type: 'workflow'` with workflow/project/jobs; invalid payloads return 400; `/api/j
```
