# Workflow Blocked Diagnostic

## Summary
- **Title:** Sentry fix [hurlicane]: WorkflowBlocked: Workflow blocked: Autonomous Agent Run: ## Test Coverage & Faul
- **ID:** 48339c5c-8b1a-4ad3-bfe8-07aeb2f72aec
- **Blocked at:** 2026-04-08T21:52:08.934Z
- **Reason:** Workflow stuck after failed implement job 5e3be874
- **Phase:** implement
- **Cycle:** 1/5
- **Milestones:** 0/4
- **Implementer model:** claude-opus-4-6
- **Reviewer model:** codex
- **Worktree:** /Users/barneyhussey-yeo/GitHub/personal/.orchestrator-worktrees/hurlicane/wf-48339c5c (branch: workflow/sentry-fix-hurlicane-workflowblocked-wor-48339c5c)

## Job History (last 10)
| ID | Phase | Status | Model | Title |
|----|-------|--------|-------|-------|
| cbb5eb10 | assess | failed | claude-opus-4-6 | [Workflow C0] Assess |
| ed576364 | assess | done | codex | [Workflow C0] Assess (fallback) |
| 82951d66 | review | done | codex | [Workflow C1] Review |
| 17abc68e | implement | failed | claude-opus-4-6 | [Workflow C1] Implement |
| 5e3be874 | implement | failed | claude-sonnet-4-6[1m] | [Workflow C1] Implement (fallback) |

## Failed Jobs (last 5 with details)
### [Workflow C0] Assess
- **Job ID:** cbb5eb10 | **Agent ID:** 3b8b8267
- **Phase:** assess | **Model:** claude-opus-4-6
- **Exit code:** n/a | **Turns used:** n/a | **Cost:** $n/a
- **DB Error:**
```
Agent session ended without a final result event or new commits.
```
- **Agent output (last lines):**
```
[ERROR] rate_limit
[ERROR] rate_limit
[ERROR] rate_limit
Now let me find the `verifyWorktreeHealth` function to understand how it handles missing worktrees and branches.
```

### [Workflow C1] Implement
- **Job ID:** 17abc68e | **Agent ID:** 269e294b
- **Phase:** implement | **Model:** claude-opus-4-6
- **Exit code:** n/a | **Turns used:** n/a | **Cost:** $n/a
- **DB Error:**
```
PTY attach failed after 4 attempts: posix_spawnp failed.
```
- **Agent output (last lines):**
```
[ERROR] rate_limit
```

### [Workflow C1] Implement (fallback)
- **Job ID:** 5e3be874 | **Agent ID:** 1d6fcd90
- **Phase:** implement | **Model:** claude-sonnet-4-6[1m]
- **Exit code:** n/a | **Turns used:** n/a | **Cost:** $n/a
- **DB Error:**
```
PTY attach failed after 4 attempts: posix_spawnp failed.
```
- **Agent output (last lines):**
```
Now let me look at the existing tests to understand coverage:
Now let me read the exact resume section to confirm the replacement boundaries:
```


## Total Job Stats
- Total: 5
- Done: 2
- Failed: 3
- Cancelled: 0
- Success rate: 40%

## Git State
### Working tree status
```
(clean)
```

### Last 3 commits
```
023a923 fix: fail fast when job work_dir does not exist (#40)
65e1aea test: cover PTY spawning-state cleanup
7f24e00 fix: clear standalone spawning state after monitor handoff
```

## Latest Worklog Entry
```
(no worklog found)
```

## Plan (truncated)
```
# Plan

## Goal
Prevent workflow startup recovery from blocking on `fatal: invalid reference` when recreating a missing shared worktree in `hurlicane`.

## Assessment
`verifyWorktreeHealth()` reaches `recreateWorktree()` from both the missing-directory and missing-`.git` repair paths. That helper still assumes `worktree_branch` already exists and always runs `git worktree add <path> <branch>`, while resume-time restoration already probes `refs/heads/<branch>` and switches between plain `git worktree add` and `git worktree add -b`. Existing coverage proves the resume path can reattach an existing branch and covers generic startup recreation failure, but it does not yet lock down the exact Sentry case or prove that `reconcileRunningWorkflows()` leaves the workflow running after a successful missing-ref repair.

## Milestones
- [ ] **M1: Centralize branch-aware worktree recreation semantics** [S] — Extract or refactor the branch-selection logic so both startup repair and resume restoration decide `git worktree add` the same way: reattach an existing `refs/heads/<branch>` without `-b`, otherwise create it with `-b` from the main repo HEAD. Acceptance criteria: one code path decides between add vs add `-b`; `recreateWorktree()` no longer assumes the branch exists; existing resume restoration behavior remains unchanged.
- [ ] **M2: Apply the shared helper to startup worktree health repair** [S] — Wire `verifyWorktreeHealth()` / `recreateWorktree()` to use the shared branch-aware helper for both missing-directory and missing-`.git` repair. Acceptance criteria: the invalid-reference Sentry path succeeds instead of returning `Worktree recreation failed`; successful startup repair emits a resilience event that indicates whether it reattached or created the branch; unrecoverable git failures still return a descriptive error that `reconcileRunningWorkflows()` can block on.
- [ ] **M3: Add low-level regression tests for startup repair branch selection** [S] — Extend `src/test/worktree-health.test.ts` to cover startup recreation against the shared helper: existing branch ref uses plain `git worktree add`, missing branch ref uses `git worktree add -b`, and a real git failure still surfaces as a failed health check. Acceptance criteria: tests assert the exact command shape for both success variants and the failure variant, and they pass without weakening existing resume-worktree expectations.
- [ ] **M4: Add reconciliation-level regression coverage for running workflows** [S] — Extend `src/test/workflow-dedup.test.ts` (or the most targeted startup-recovery suite) with a running-workflow scenario where startup health repair succeeds after the branch ref is missing, plus a control case where unrecoverable recreation failure still marks the workflow blocked with the startup health-check reason. Acceptance criteria: one test proves `reconcileRunningWorkflows()` keeps the workflow running with no startup block on successful repair, and one test proves the blocked pat
```
