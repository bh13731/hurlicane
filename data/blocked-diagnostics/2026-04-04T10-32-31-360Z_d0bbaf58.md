# Workflow Blocked Diagnostic

## Summary
- **Title:** Unify dashboard: single Tasks section replacing dual grids
- **ID:** d0bbaf58-7083-4f26-bdbc-4a326c0d1dd9
- **Blocked at:** 2026-04-04T10:32:31.390Z
- **Reason:** PR creation failed — worktree preserved for retry at /Users/barneyhussey-yeo/GitHub/personal/.orchestrator-worktrees/hurlicane/wf-d0bbaf58
- **Phase:** idle
- **Cycle:** 6/15
- **Milestones:** 6/6
- **Implementer model:** claude-opus-4-6
- **Reviewer model:** codex-gpt-5.4
- **Worktree:** /Users/barneyhussey-yeo/GitHub/personal/.orchestrator-worktrees/hurlicane/wf-d0bbaf58 (branch: workflow/unify-dashboard-single-tasks-section-rep-d0bbaf58)

## Job History (last 10)
| ID | Phase | Status | Model | Title |
|----|-------|--------|-------|-------|
| 076e962d | review | done | codex-gpt-5.4 | [Workflow C2] Review |
| 66388455 | implement | done | claude-opus-4-6 | [Workflow C2] Implement |
| cb850ec2 | review | done | codex-gpt-5.4 | [Workflow C3] Review |
| 34914562 | implement | done | claude-opus-4-6 | [Workflow C3] Implement |
| 1baf8e95 | review | done | codex-gpt-5.4 | [Workflow C4] Review |
| 95989d6d | implement | done | claude-opus-4-6 | [Workflow C4] Implement |
| d0ba4b89 | review | done | codex-gpt-5.4 | [Workflow C5] Review |
| 082f63e8 | implement | done | claude-opus-4-6 | [Workflow C5] Implement |
| 8492290e | review | done | codex-gpt-5.4 | [Workflow C6] Review |
| 2ac32889 | implement | done | claude-opus-4-6 | [Workflow C6] Implement |

## Failed Jobs (last 5 with details)
No failed jobs.

## Total Job Stats
- Total: 13
- Done: 13
- Failed: 0
- Cancelled: 0
- Success rate: 100%

## Git State
### Working tree status
```
(clean)
```

### Last 3 commits
```
4f3cc5f feat: replace dual dashboard sections with unified TaskFeed (M6)
681df53 feat: apply customOrder drag-reorder to standalone agent items in TaskFeed (M5)
7f42ce3 fix: use live clock for task grouping and fix filtered empty state (M4)
```

## Latest Worklog Entry
```
## Cycle 6 — M6: Replace the dual dashboard sections and verify live behavior
**Owner:** Implementer
**Timestamp:** 2026-04-04T11:33:00Z

### What changed
- `src/client/App.tsx`: Removed imports of `AgentGrid` and `AutonomousRunDashboard`, replaced with `TaskFeed` import. Removed `singleRunAgents` and `queuedSingleJobs` useMemo hooks (no longer needed — TaskFeed's model handles deduplication internally). Replaced the two `<section>` blocks ("Autonomous Runs" + "Single Jobs") with a single "Tasks" section containing `<TaskFeed>`. Props passed: `filteredWorkflows`, `filteredAgents` (model deduplicates workflow-owned agents), `agents` as `allAgents` (for WorkflowSummaryCard agent lookup), `filteredJobs`, all queued filtered jobs (model deduplicates workflow-owned queued jobs), `dashboardNow`, selection/archive handlers, PTY idle set, and archived flag. Archived load-more button preserved outside the TaskFeed section.

### Commits
- `4f3cc5f` feat: replace dual dashboard sections with unified TaskFeed (M6)

### Test results
- Full suite: 827 passed (61 files), 0 failed
- Vite client build: success (no new errors)

### Blockers
- None

### Next step
All milestones complete
```

## Plan (truncated)
```
# Plan

## Goal
Replace the two separate dashboard sections ("Autonomous Runs" + "Single Jobs") with a single unified "Tasks" section that groups workflows and standalone jobs by urgency without regressing existing dashboard behavior.

## Assessment

**Current state:**
- `src/client/taskFeedModel.ts` still enforces the intended data contract internally: it excludes workflow-owned agents/jobs when the parent workflow is present, suppresses queued-job duplicates when an agent already exists for that job, preserves queued standalone jobs without agents, and keeps grouping/sorting pure and deterministic.
- `src/test/task-feed-model.test.ts` still covers the model-layer overlap cases directly.
- `src/client/components/TaskFeed.tsx` + `TaskFeed.module.css` exist and reuse `WorkflowSummaryCard`, `AgentCard`, and the queued-job card pattern from `AgentGrid`.
- `App.tsx` now renders a single "Tasks" section using `TaskFeed` instead of separate `AutonomousRunDashboard` and `AgentGrid` sections.

**Cycle 6 update:**
- M6 replaced the two dashboard sections in `App.tsx` with a single `<TaskFeed>` section. Removed imports of `AgentGrid` and `AutonomousRunDashboard`. Removed unused `singleRunAgents` and `queuedSingleJobs` memos. Passed `filteredAgents` and all queued `filteredJobs` to TaskFeed (model handles deduplication). Archived load-more controls preserved. Full test suite passes (827/827). Vite build succeeds.

## Milestones
- [x] **M1: Define unified task-item model and urgency grouping** [M]
- [x] **M2: Fix task-feed model deduplication and workflow exclusion guarantees** [M]
- [x] **M3: Build `TaskFeed` UI with existing cards and preserved controls** [L]
- [x] **M4: Fix TaskFeed live-state and filtered-empty regressions** [M]
- [x] **M5: Restore standalone drag-reorder behavior inside `TaskFeed`** [M]
- [x] **M6: Replace the dual dashboard sections and verify live behavior** [M]

## Priority Order
All milestones complete.

## Risks
All mitigated — the unified TaskFeed is now the sole dashboard section, deduplication is model-driven, live ticking is handled by TaskFeed's internal `useNowTick`, and archived mode is preserved.
```
