services:
  orchestrator:
    build: .
    ports:
      - "3000:3000"
      - "3001:3001"
    volumes:
      - orchestrator-data:/app/data
    environment:
      - AUTH_PASSWORD=${AUTH_PASSWORD:-}
    restart: unless-stopped
    stop_grace_period: 15s

volumes:
  orchestrator-data:
